# 03. 실습 — Nginx 연결·비공개·캐시·보호·장애 복구

3주차의 PostgreSQL 이미지·환경 변수·Named Volume은 유지한다. 이 문서는 기존 파일을 통째로 덮어쓰지 않고, **추가하거나 교체할 부분만** 보여 준다.

## 이 문서를 보는 방법

실습에는 다음 화면을 사용한다. 코드 블록 위의 **입력·수정/실행 위치**를 먼저 확인한다.

| 표시 | 실제로 작업할 곳 | 하는 일 |
| --- | --- | --- |
| 터미널 A — 로그 관찰 | WSL2 Ubuntu에서 **PetClinic 폴더**로 이동한 첫 번째 터미널 | `docker compose logs -f`를 계속 띄워 로그 관찰 |
| 터미널 B — 명령 실행 | 같은 PetClinic 폴더를 연 두 번째 터미널 | `docker`, `curl`, `mkdir` 같은 명령 입력 |
| nano 편집 화면 | 터미널 B에서 `nano 파일경로`를 실행하면 열리는 화면 | YAML·Nginx 설정·HTML 추가 및 수정 |
| PC 브라우저 | Chrome·Edge·Safari 등 | `http://localhost:8080` 화면 확인 |
| 휴대폰 브라우저 | 같은 Wi-Fi에 연결된 휴대폰 | 선택 외부 접속 시험 |

터미널을 두 개 쓰는 이유는 역할이 다르기 때문이다. 터미널 A는 실행 로그가 계속 출력되어 다른 명령을 입력하기 불편하므로 **관찰용**으로 두고, 터미널 B에서 요청·설정 검사 같은 **실행 명령**을 입력한다. 서로 다른 서버가 아니라 같은 컴퓨터의 같은 PetClinic 폴더를 보여 주는 창 두 개다.

이번 실습의 PetClinic은 `docker compose up -d`로 백그라운드 실행한다. 따라서 터미널 A를 닫아도 앱 컨테이너가 바로 종료되지는 않는다. 터미널 A는 로그를 편하게 보기 위한 선택 사항이며, 터미널 하나만 쓴다면 `logs -f`를 확인한 뒤 `Ctrl+C`로 로그 보기만 끝내고 다음 명령을 입력해도 된다.

터미널 두 개를 열고 모두 **3주차에서 사용한 PetClinic 폴더**로 이동한다. 이후 별도 안내가 없는 `bash` 명령은 터미널 B에 입력한다.

```bash
# 현재 위치가 PetClinic 폴더인지 파일 목록으로 확인한다.
ls
```

정상이면 `Dockerfile`, `docker-compose.yml`, `src` 등이 보인다. 개인마다 저장 위치가 다르므로 이 문서에서는 절대경로나 사용자명을 사용하지 않고 이 위치를 모두 **PetClinic 폴더**라고 부른다. 명령 앞의 `$`나 설명용 주석은 직접 입력할 필요가 없다.

파일을 수정할 때는 터미널 B에서 안내된 `nano 파일경로`를 실행한다. nano 화면에서 내용을 수정한 뒤 다음 순서로 저장하고 나온다.

```text
Ctrl+O → Enter : 저장
Ctrl+X         : nano 종료
```

> **화면 표시 원칙**  
> 각 단계는 `검사 목표 → 입력·수정 → 명령 한눈에 보기 → 결과·로그 판정 → 짧은 설명` 순서로 읽는다. 코드 블록의 주석은 **명령 전체의 목적**을 설명하고, 아래 표는 처음 보면 해석하기 어려운 핵심 구성만 설명한다. 같은 `curl -sS`처럼 이미 설명한 기초 옵션은 매번 길게 반복하지 않는다.

### 여러 명령이 한 줄에 연결됐을 때 읽는 법

| 표기 | 읽는 방법 |
| --- | --- |
| `명령1 \| 명령2` | 명령1의 출력을 명령2의 입력으로 넘긴다. 왼쪽부터 읽는다. |
| 줄 끝의 `\` | 화면에서는 줄을 나눴지만 다음 줄까지 하나의 명령이라는 뜻이다. |
| `$ASSET`·`$RATE_TEST_ID` | 앞에서 변수에 저장한 값을 꺼내 쓴다. |
| `sh -c '...'` | 따옴표 안의 여러 명령을 하나의 셸 명령 묶음으로 실행한다. 이 자료에서는 주로 컨테이너 안에서 로그 검색 파이프라인을 실행할 때 쓴다. |
| `&&` | 앞 명령이 성공했을 때만 뒤 명령을 실행한다. |
| `\|\|` | 앞 명령이 실패했을 때 뒤 명령을 실행한다. |

각 단계에서는 먼저 코드 위 주석으로 **무엇을 시험하는 명령인지** 확인하고, 문법이 궁금할 때만 아래의 `명령 한눈에 보기`를 읽는다.

## 목차

- 시작 전. 이론의 구성 요소를 실습 서비스에 대응하기
- 0. 실습 공통: 응답과 로그로 성공 여부 판단하기
- 1. Compose에 Nginx·app을 추가해 함께 실행하기
- 2. app·PostgreSQL은 감추고 Nginx만 공개하기
- 3. 정적 파일 캐시 효과 비교하기
- 4. Nginx 앞단에서 요청 제한·보호하기
- 5. PetClinic 장애 확인·안내·복구
- 종료

---

## 시작 전. 이론의 구성 요소를 실습 서비스에 대응하기

이론에서 구분한 프로그램은 Compose에서 다음 서비스로 실행한다.

| 이론의 역할 | 실습 대상 |
| --- | --- |
| 웹 서버·리버스 프록시 | `nginx` 서비스 |
| 웹 애플리케이션 | PetClinic의 Spring 코드 |
| WAS | `app` 안의 내장 Tomcat |
| 앱 서버 | PetClinic과 내장 Tomcat이 함께 실행되는 `app` 서비스 |
| DB 서버 | `postgres` 서비스 |

이론 마지막의 “Docker를 사용하지 않는 회사 환경의 연결 방식”은 현재 Compose 구조와 비교하기 위한 참고 내용이므로 별도 실습은 하지 않는다.

---

## 0. 실습 공통: 응답과 로그로 성공 여부 판단하기

> **이 단계에서 검사하는 것:** 서비스별 최근 로그와 새 로그를 골라 보는 공통 명령의 사용 방법을 확인한다.

**입력·실행 — 터미널 B · 위치: PetClinic 폴더**

```bash
# 세 서비스의 가장 최근 로그 30줄만 모아 기동·오류 상태를 빠르게 확인한다.
docker compose logs --tail 30 nginx app postgres

# 최근 2분 동안 발생한 Nginx 로그만 좁혀서 확인한다.
docker compose logs --since 2m nginx
```

**입력·실행 — 터미널 A**

```bash
# 새로 생기는 Nginx 로그를 실시간으로 확인한다.
docker compose logs -f nginx
```

**명령 한눈에 보기**

| 핵심 구성 | 이 명령에서 하는 역할 |
| --- | --- |
| `logs --tail 30` | 각 서비스의 마지막 로그 30줄만 본다. |
| `logs --since 2m nginx` | 최근 2분의 Nginx 로그만 본다. |
| `logs -f nginx` | 새 Nginx 로그를 실시간으로 계속 본다. |
| `Ctrl+C` | 로그 보기만 끝내며 컨테이너는 중지하지 않는다. |

**결과·로그 판정**

첫 명령은 서비스별 마지막 로그, 두 번째는 최근 Nginx 로그를 출력한다. 세 번째는 새 로그를 기다리며 계속 실행되는 것이 정상이다. 이 명령들은 컨테이너를 실행한 1단계 이후 사용한다.

**짧은 설명**

각 시험 요청에 `?study=step번호-이름`을 붙이고 필요한 로그만 검색한다. `curl`은 사용자가 받은 결과, Nginx access log는 상태·캐시·app 전달, Nginx error log는 오류 원인, Tomcat access log는 app 도달, PostgreSQL 로그는 DB 준비 상태를 보여 준다.

---

## 1. Compose에 Nginx·app을 추가해 함께 실행하기

### 1-1. 현재 구성 확인과 원본 백업

> **이 단계에서 검사하는 것:** 수정할 Compose 파일이 있는지 확인하고, 문제가 생겼을 때 돌아갈 원본 백업을 만든다.

**입력·수정 — 터미널 B · 위치: PetClinic 폴더**

```bash
# Docker Desktop의 Engine과 현재 터미널이 실제로 연결됐는지 확인한다.
docker info >/dev/null && echo "Docker Engine 연결 정상"
# 2주차에서 만든 PetClinic 이미지가 남아 있는지 확인한다.
docker image inspect spring-petclinic:dockerfile >/dev/null && \
  echo "PetClinic 이미지 확인"
# 수정할 Compose 파일 두 개가 현재 폴더에 있는지 확인한다.
ls -l docker-compose.yml docker-compose.override.yml

# 백업 파일 이름에 붙일 현재 날짜와 시각을 만든다.
BACKUP_SUFFIX=$(date +%Y%m%d-%H%M%S)
# 기본 Compose 파일을 수정 전 상태로 복사한다.
cp docker-compose.yml "docker-compose.yml.before-week4-$BACKUP_SUFFIX"
# override Compose 파일을 수정 전 상태로 복사한다.
cp docker-compose.override.yml "docker-compose.override.yml.before-week4-$BACKUP_SUFFIX"
# 두 백업 파일이 실제로 만들어졌는지 확인한다.
ls *before-week4-$BACKUP_SUFFIX
```

**명령 한눈에 보기**

| 핵심 구성 | 이 명령에서 하는 역할 |
| --- | --- |
| `docker info` | Docker Desktop의 Engine이 실행 중이고 현재 터미널에서 사용할 수 있는지 확인한다. |
| `docker image inspect ...` | Compose의 `app.image`에 적을 PetClinic 이미지가 로컬에 있는지 확인한다. |
| 첫 번째 `ls -l` | 수정할 기본·override Compose 파일이 모두 있는지 확인한다. |
| `date +%Y%m%d-%H%M%S` | 현재 시각을 `20260802-153000` 같은 글자로 만든다. |
| `BACKUP_SUFFIX=...` | 그 글자를 두 백업 파일이 함께 사용할 이름으로 저장한다. |
| `cp 원본 백업본` | 원본은 그대로 두고 같은 내용의 백업 파일을 만든다. |
| `ls *before-week4-...` | 실제로 생성된 백업 파일 두 개를 보여 준다. |

**결과·로그 판정**

먼저 `Docker Engine 연결 정상`, `PetClinic 이미지 확인`과 Compose 파일 두 개가 보여야 한다. 여기서 오류가 나면 파일을 수정하지 말고 Docker Desktop 실행·WSL 연동 또는 2주차 이미지부터 복구한다. 마지막 출력에 다음처럼 백업 파일 두 개가 보이면 정상이다.

```text
docker-compose.yml.before-week4-날짜
docker-compose.override.yml.before-week4-날짜
```

**짧은 설명**

이후 두 Compose 파일을 수정하므로, 문제가 생기면 원본으로 돌아갈 수 있도록 먼저 같은 내용의 복사본을 남긴다.

### 1-2. PostgreSQL의 기존 데이터 유지와 준비 상태 확인

> **이 단계에서 검사하는 것:** 3주차 PostgreSQL 데이터 볼륨을 그대로 유지하면서, DB가 실제 연결을 받을 준비가 된 뒤 app이 시작되도록 설정한다.

**입력·수정 — 터미널 B**

```bash
# 실행 중인 기존 Compose 컨테이너를 종료하되 DB 볼륨은 보존한다.
docker compose down
```

기존 `postgres` 서비스의 `ports: ["5432:5432"]`는 삭제하고, 기존 `image`·`environment`는 유지한다.

```bash
# 기본 Compose 파일을 nano 편집기로 연다.
nano docker-compose.yml
```

nano 화면에서 `services.postgres`를 찾아 아래 내용을 추가한다.

```yaml
# postgres 서비스에 추가
expose: ["5432"]
networks: [db-net]
healthcheck:
  test: ["CMD-SHELL", "pg_isready -U petclinic -d petclinic"]
  interval: 5s
  timeout: 5s
  retries: 20
```

**명령·설정 한눈에 보기**

| 설정 | 이 실습에서 하는 역할 |
| --- | --- |
| `expose` | PostgreSQL이 컨테이너 내부에서 5432를 사용한다고 표시한다. 호스트에 포트를 열지는 않는다. |
| `networks` | PostgreSQL을 app과 통신할 `db-net`에 연결한다. |
| `healthcheck` | PostgreSQL이 실제 연결을 받을 준비가 됐는지 반복 검사한다. |
| `test` | `pg_isready`로 `petclinic` 사용자와 DB의 준비 상태를 검사한다. |
| `interval` | 검사를 5초마다 실행한다. |
| `timeout` | 한 번의 검사가 5초 안에 끝나지 않으면 실패로 처리한다. |
| `retries` | 최대 20번 연속 실패하면 컨테이너를 `unhealthy`로 표시한다. |

**결과·로그 판정**

여기서는 저장만 한다. YAML 오류와 DB 준비 상태는 1-8의 `docker compose config`와 `pg_isready`에서 확인한다.

**짧은 설명**

`docker compose down`에는 `-v`를 붙이지 않아 기존 DB 볼륨을 보존한다. 4주차에서 사용하지 않는 `mysql` 설정은 삭제하지 않고, 이후 실행 대상에서만 제외한다.

### 1-3. 새 `app` 서비스 추가

> **이 단계에서 검사하는 것:** PetClinic을 Compose의 `app` 컨테이너로 실행하고, 내부 주소 `postgres:5432`로 DB에 연결하며 Tomcat 요청 로그를 남기게 한다.

**입력·수정 — 터미널 B**

```bash
# app 서비스를 추가·수정하기 위해 기본 Compose 파일을 연다.
nano docker-compose.yml
```

nano 화면에서 `services:` 아래에 다음 `app:` 블록을 추가한다.

```yaml
app:
  image: spring-petclinic:dockerfile
  environment:
    SPRING_PROFILES_ACTIVE: postgres
    POSTGRES_URL: jdbc:postgresql://postgres:5432/petclinic
    POSTGRES_USER: petclinic
    POSTGRES_PASS: petclinic
    SPRING_SQL_INIT_MODE: never
    SERVER_TOMCAT_BASEDIR: /tmp/tomcat
    SERVER_TOMCAT_ACCESSLOG_ENABLED: "true"
    SERVER_TOMCAT_ACCESSLOG_DIRECTORY: access
    SERVER_TOMCAT_ACCESSLOG_PATTERN: common
    SERVER_TOMCAT_ACCESSLOG_BUFFERED: "false"
  expose: ["8080"]
  volumes:
    - ./logs/tomcat:/tmp/tomcat
  depends_on:
    postgres:
      condition: service_healthy
  networks: [proxy-net, db-net]
```

**명령·설정 한눈에 보기**

| 설정 | 이 실습에서 하는 역할 |
| --- | --- |
| `app` | Compose 내부에서 PetClinic 컨테이너를 부를 서비스 이름이다. Nginx도 이 이름을 사용한다. |
| `image` | 앞 주차에서 만든 PetClinic 이미지를 사용한다. |
| `SPRING_PROFILES_ACTIVE` | PetClinic이 PostgreSQL용 설정을 선택하게 한다. |
| `POSTGRES_URL` | app이 접속할 DB 주소다. `postgres`는 DB 서비스명이고 `5432`는 DB 컨테이너 내부 포트다. |
| `POSTGRES_USER`, `POSTGRES_PASS` | 3주차 PostgreSQL에 설정한 계정 정보다. |
| `SPRING_SQL_INIT_MODE` | 이미 만들어진 3주차 테이블을 다시 생성하지 않게 한다. |
| `SERVER_TOMCAT_...` | PetClinic 안의 Tomcat이 받은 HTTP 요청을 파일로 기록하게 한다. |
| `expose` | app이 내부에서 8080을 사용한다고 표시한다. 호스트에는 열지 않는다. |
| `volumes` | Tomcat 로그를 PetClinic 폴더의 `logs/tomcat`에서도 볼 수 있게 연결한다. |
| `depends_on` | PostgreSQL healthcheck가 성공한 뒤 app을 시작한다. |
| `networks` | Nginx와 통신하는 `proxy-net`, DB와 통신하는 `db-net`에 app을 모두 연결한다. |

**결과·로그 판정**

저장 후 `app:`이 `postgres:` 안쪽이 아니라 `services:` 바로 아래에서 같은 깊이인지 확인한다. 1-8의 `docker compose config`에서 `app`, `postgres:5432`, 두 네트워크가 보이고 app 로그에 `Started ...`가 나오면 정상이다.

**짧은 설명**

`app` 블록에는 Tomcat 로그 설정까지 포함한다. 컨테이너 안의 `localhost`는 app 자신이므로 DB 주소는 `postgres:5432`를 사용하며, `SPRING_SQL_INIT_MODE: never`는 3주차에서 이미 생성된 DB를 이어 쓸 때만 사용한다.

### 1-4. `nginx` 서비스와 내부 네트워크 추가

> **이 단계에서 검사하는 것:** Nginx만 호스트 `8080`을 공개하고, Nginx→app→PostgreSQL 흐름이 내부 네트워크로만 이어지도록 구성한다.

**입력·수정 — 터미널 B**

```bash
# nginx 서비스와 네트워크·볼륨을 추가하기 위해 기본 Compose 파일을 연다.
nano docker-compose.yml
```

nano 화면에서 `services:` 아래에 다음 `nginx:` 블록을 추가한다.

```yaml
nginx:
  image: nginx:stable-alpine
  ports: ["8080:80"]
  volumes:
    - ./nginx/conf.d:/etc/nginx/conf.d:ro
    - nginx-cache:/var/cache/nginx
  depends_on: [app]
  networks: [proxy-net]
```

**명령·설정 한눈에 보기**

| 설정 | 이 실습에서 하는 역할 |
| --- | --- |
| `nginx` | Compose 내부에서 Nginx 컨테이너를 부를 서비스 이름이다. |
| `image` | 공식 Nginx 이미지를 사용한다. |
| `ports: ["8080:80"]` | PC의 8080으로 받은 요청을 Nginx 컨테이너의 80으로 전달한다. 이번 구성에서 호스트에 여는 유일한 포트다. |
| 첫 번째 `volumes` | PetClinic 폴더에서 작성한 Nginx 설정을 컨테이너가 읽게 한다. `ro`는 읽기 전용이다. |
| `nginx-cache` | Nginx가 저장한 캐시 파일을 보관한다. |
| `depends_on` | app 컨테이너를 먼저 시작한다. |
| `networks` | Nginx를 app과 통신할 `proxy-net`에만 연결한다. |

`docker-compose.yml` 맨 아래에 다음 최상위 항목을 추가한다. `docker-compose.override.yml`의 `petclinic-postgres-data` 선언은 수정하지 않는다. Compose가 두 파일을 합치면 PostgreSQL 볼륨과 `nginx-cache`가 함께 남는다.

같은 nano 화면에서 파일 맨 아래의 `services:` 바깥쪽에 다음 내용을 추가한다. 앞에서 nano를 닫았다면 `nano docker-compose.yml`로 다시 연다.

```yaml
networks:
  proxy-net:
  db-net:
    internal: true

volumes:
  nginx-cache:
```

**네트워크·볼륨 설정 한눈에 보기**

| 설정 | 이 실습에서 하는 역할 |
| --- | --- |
| `proxy-net` | Nginx와 app이 요청을 주고받는 네트워크다. |
| `db-net` | app과 PostgreSQL이 통신하는 네트워크다. |
| `internal: true` | `db-net`을 DB 통신용 내부 네트워크로 제한한다. |
| `volumes.nginx-cache` | Nginx 캐시가 사용할 Named Volume을 만든다. |

**결과·로그 판정**

여기서는 저장만 한다. 1-8의 `docker compose config`에서 `nginx`에만 `8080:80`이 있고, `app`·`postgres`에는 `ports`가 없으며 두 네트워크가 보이면 정상이다.

**짧은 설명**

`proxy-net`은 Nginx와 app 사이, `db-net`은 app과 PostgreSQL 사이의 통신망이다. 캐시 파일은 `nginx-cache` 볼륨에 보존한다.

### 1-5. Tomcat 로그 폴더 만들기

> **이 단계에서 검사하는 것:** 요청이 Nginx에서 끝났는지 PetClinic의 내장 Tomcat까지 도달했는지 비교할 앱 측 로그 저장 위치를 준비한다.

**입력·수정 — 터미널 B**

```bash
# PetClinic 내부 Tomcat(WAS)의 HTTP 요청 로그가 저장될 폴더를 만든다.
mkdir -p logs/tomcat/access
```

**명령 한눈에 보기**

| 핵심 구성 | 이 명령에서 하는 역할 |
| --- | --- |
| `mkdir` | 새 폴더를 만든다. |
| `-p` | 상위 폴더가 없어도 함께 만들고, 이미 있어도 오류를 내지 않는다. |
| `logs/tomcat/access` | PetClinic 안의 Tomcat 요청 로그가 저장될 호스트 폴더다. |

**결과·로그 판정**

성공하면 아무 메시지도 나오지 않는다. `ls -ld logs/tomcat/access`에서 폴더 정보가 보이면 정상이다.

**짧은 설명**

app 컨테이너의 `/tmp/tomcat/access`와 이 폴더가 연결된다. `buffered=false`를 사용하므로 요청 직후 Tomcat 로그를 확인할 수 있다. [Spring Boot 속성 문서](https://docs.spring.io/spring-boot/appendix/application-properties/index.html)

### 1-6. Nginx 로그 형식 파일 만들기

> **이 단계에서 검사하는 것:** 상태코드·캐시 상태·요청 제한·처리 시간·app 전달 주소가 한 줄에 보이도록 학습용 Nginx 로그 형식을 정의한다.

**입력·수정 — 터미널 B**

```bash
# Nginx 설정 파일을 보관할 폴더를 만든다.
mkdir -p nginx/conf.d
# 새 공통 설정 파일을 만든다. nano는 파일이 없으면 저장할 때 새로 만든다.
nano nginx/conf.d/00-observability.conf
```

nano 화면에 아래 전체 내용을 입력하고 `Ctrl+O → Enter → Ctrl+X` 순서로 저장·종료한다.

```nginx
log_format study 'time=$time_iso8601 status=$status method=$request_method '
                 'uri="$request_uri" cache=$upstream_cache_status '
                 'rate_limit=$limit_req_status request_time=$request_time '
                 'upstream_connect=$upstream_connect_time '
                 'upstream_header=$upstream_header_time '
                 'upstream_time=$upstream_response_time upstream="$upstream_addr"';
error_log /dev/stderr notice;
```

**명령·설정 한눈에 보기**

| 로그 변수 | 기록하는 값 |
| --- | --- |
| `mkdir -p nginx/conf.d` | 프로젝트 안에 Nginx 설정 파일을 보관할 폴더를 만든다. |
| `nano .../00-observability.conf` | 존재하지 않는 공통 설정 파일을 새로 작성한다. 별도의 `touch`나 기존 파일 검사는 하지 않는다. |
| `log_format study` | 아래 로그 항목들을 `study`라는 형식 이름으로 묶는다. |
| `time` | 요청을 처리한 시각 |
| `status` | 사용자에게 반환한 HTTP 상태 코드 |
| `method`·`uri` | 요청 방식과 요청 주소 |
| `cache` | 캐시 결과인 `BYPASS`, `MISS`, `HIT` |
| `rate_limit` | 요청 제한이 적용됐는지 여부 |
| `request_time` | Nginx가 요청 전체를 처리한 시간 |
| `upstream_connect` | Nginx가 app에 TCP 연결을 만드는 데 걸린 시간 |
| `upstream_header` | Nginx가 app에서 응답 헤더를 받는 데 걸린 시간 |
| `upstream_time`·`upstream` | Nginx가 app의 전체 응답을 받는 데 걸린 시간과 실제 전달 대상 |
| `error_log ... notice` | Nginx 오류를 컨테이너 로그에 `notice` 수준부터 남긴다. |

**결과·로그 판정**

저장 직후에는 별도 결과가 출력되지 않는다. `ls -l nginx/conf.d/00-observability.conf`에서 파일이 보이면 작성된 것이며, 실제 문법은 1-8의 `nginx -t`로 판정한다.

**짧은 설명**

이 파일은 여러 요청 경로가 공통으로 사용할 로그 형식만 정의한다. 실제 로그 적용은 다음 단계의 `access_log /dev/stdout study;`가 담당한다.

### 1-7. Nginx 웹 서버 설정 파일 만들기

> **이 단계에서 검사하는 것:** Nginx가 컨테이너 내부 80번 포트에서 요청을 받고 `app:8080`으로 전달하며, 앞에서 정의한 학습용 로그를 실제로 기록하도록 설정한다.

**입력·수정 — 터미널 B**

```bash
# Nginx의 포트·프록시·요청 경로 규칙을 입력하기 위해 웹 서버 설정 파일을 연다.
nano nginx/conf.d/default.conf
```

nano 화면에 아래 전체 내용을 입력하고 저장·종료한다.

```nginx
server {
  listen 80;
  access_log /dev/stdout study;

  location / {
    proxy_pass http://app:8080;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
  }
}
```

**명령·설정 한눈에 보기**

| 설정 | 이 실습에서 하는 역할 |
| --- | --- |
| `nano .../default.conf` | 새 웹 서버 설정 파일을 작성한다. 파일이 없으면 nano가 저장할 때 생성한다. |
| `server`·`listen 80` | Nginx 컨테이너의 80번 포트에서 요청을 받는다. |
| `access_log ... study` | 1-6에서 정의한 `study` 형식으로 요청 로그를 남긴다. |
| `location /` | 일반 URL 요청에 적용할 규칙을 묶는다. |
| `proxy_pass http://app:8080` | 요청을 Compose 내부의 PetClinic app으로 전달한다. |
| `proxy_set_header` | 원래 요청의 호스트·IP·프로토콜 정보를 app에 전달한다. |

**결과·로그 판정**

저장 직후에는 출력이 없다. 파일 존재 여부는 `ls -l nginx/conf.d/default.conf`, 문법은 1-8의 `nginx -t`로 확인한다.

**짧은 설명**

`00-observability.conf`는 공통 로그 형식을 정의하고, `default.conf`는 실제로 요청을 받아 app으로 전달하는 규칙을 정의한다.

### 1-8. 설정 검사와 세 서비스 실행

> **이 단계에서 검사하는 것:** 필요한 파일·서비스와 설정 문법을 확인한 뒤 PostgreSQL→app→Nginx 순서로 정상 기동하고 첫 요청·로그를 검증한다.

**입력·실행 — 터미널 B**

```bash
# 합쳐진 Compose 설정의 문법 오류가 없는지 확인한다.
docker compose config -q && echo "Compose 설정 정상"
# DB와 PetClinic 앱을 먼저 백그라운드에서 실행한다.
docker compose up -d postgres app
```

**입력·실행 — 터미널 A**

```bash
# app이 완전히 기동되어 Started 문구가 나오는지 실시간으로 확인한다.
docker compose logs -f --since 1m app
```

`Started ...`가 보이면 `Ctrl+C`로 로그 보기만 끝낸다.

**입력·실행 — 터미널 B**

```bash
# 실제 Nginx를 띄우기 전에 임시 컨테이너에서 설정 문법을 검사한다.
docker compose run --rm --no-deps nginx nginx -t
# 문법 검사가 성공한 Nginx를 백그라운드에서 실행한다.
docker compose up -d nginx
# 세 서비스의 실행 상태와 호스트 공개 포트를 확인한다.
docker compose ps
# Nginx의 호스트 포트로 요청해 PetClinic이 200으로 열리는지 확인한다.
curl -i http://localhost:8080/
# petclinic 사용자·DB 기준으로 PostgreSQL이 연결을 받을 준비가 됐는지 확인한다.
docker compose exec postgres pg_isready -U petclinic -d petclinic
# 로그에서 쉽게 찾을 수 있는 표식을 붙여 Nginx에 요청 한 건을 보낸다.
curl -sS -o /dev/null 'http://localhost:8080/?study=step1-log-once'
# 최근 Nginx 로그 중 방금 보낸 표식이 있는 요청만 골라 로그 형식을 확인한다.
docker compose logs --since 30s --no-log-prefix nginx | \
  grep 'study=step1-log-once'
```

**명령 한눈에 보기**

| 전체 명령·핵심 구성 | 하는 일 |
| --- | --- |
| `docker compose config -q` | 합쳐진 Compose YAML 문법을 검사한다. |
| `docker compose up -d postgres app` | DB와 app을 백그라운드에서 먼저 실행한다. |
| `docker compose logs -f --since 1m app` | 최근 app 로그를 실시간으로 보며 `Started ...`를 기다린다. |
| `docker compose run ... nginx -t` | 임시 Nginx 컨테이너로 설정 문법을 검사한다. 검사 후 임시 컨테이너는 삭제된다. |
| `docker compose up -d nginx` | 실제 Nginx 컨테이너를 백그라운드로 실행한다. |
| `docker compose ps` | 세 서비스의 실행 상태와 호스트에 공개된 포트를 확인한다. |
| `curl` | 지정한 주소로 HTTP 요청을 보낸다. |
| `-i` | 응답 본문 앞에 HTTP 상태와 응답 헤더도 함께 출력한다. |
| `-s` | 진행률과 일반 안내 문구를 숨긴다. |
| `-S` | `-s`를 사용해도 오류 메시지는 표시한다. `-sS`는 두 옵션을 함께 쓴 표기다. |
| `-o /dev/null` | 응답 본문을 화면이나 파일에 남기지 않고 버린다. 여기서는 로그 생성만 필요하기 때문이다. |
| `?study=step1-log-once` | 실제 기능에 영향을 주지 않는 시험 표식을 URL에 붙여 로그에서 해당 요청만 찾게 한다. |
| `pg_isready -U petclinic -d petclinic` | `petclinic` 사용자·DB 기준으로 PostgreSQL이 연결을 받을 준비가 됐는지 확인한다. |
| `?study=step1-log-once` | 방금 보낸 요청만 로그에서 찾기 위한 시험 표식이다. |

**결과·로그 판정**

| 확인 항목 | 정상 결과 |
| --- | --- |
| Compose 문법 | `Compose 설정 정상` |
| app 로그 | `Started ...` |
| Nginx 문법 | `syntax is ok`, `test is successful` |
| 실행 상태 | `postgres`, `app`, `nginx`가 실행 중이고 Nginx에만 `8080->80` 표시 |
| HTTP 응답 | `HTTP/1.1 200` |
| DB 상태 | `accepting connections` |
| Nginx 로그 | `status=200`, `study=step1-log-once`, `upstream="...:8080"` |

**짧은 설명**

Compose 문법을 먼저 검사한 뒤 DB와 app을 기동하고, app이 준비된 다음 Nginx 문법을 검사해 실행한다. 설정 폴더가 읽기 전용이라 시작 로그에 `can not modify ... read-only`가 보여도 `nginx -t`가 성공하고 컨테이너가 실행 중이면 실패가 아니다.

---

## 2. app·PostgreSQL은 감추고 Nginx만 공개하기

### 2-1. 공개 주소와 내부 주소 확인

> **이 단계에서 검사하는 것:** 사용자는 `localhost:8080`의 Nginx로 접속하고, app과 PostgreSQL은 Compose 내부 주소로만 통신하는지 확인한다.

**입력·실행 — 터미널 B**

```bash
# 현재 Compose 설정에 문법 오류가 없는지 먼저 확인한다.
docker compose config -q && echo "Compose 설정 정상"
# 포트·네트워크 변경 사항을 적용하도록 세 컨테이너를 다시 만든다.
docker compose up -d --force-recreate app postgres nginx
```

**명령 한눈에 보기**

| 핵심 구성 | 이 명령에서 하는 역할 |
| --- | --- |
| `config -q` | Compose 문법을 조용히 검사한다. 정상일 때만 뒤의 문구가 출력된다. |
| `up -d` | 지정한 서비스를 백그라운드로 실행한다. |
| `--force-recreate` | 기존 컨테이너가 있어도 지금 저장한 포트·네트워크 설정으로 다시 만든다. |

**입력·실행 — 터미널 A**

```bash
# 재생성된 app이 Started 상태까지 정상 기동하는지 실시간으로 확인한다.
docker compose logs -f --since 1m app
```

| 핵심 구성 | 이 명령에서 하는 역할 |
| --- | --- |
| `logs -f app` | app의 새 로그를 계속 보여 준다. |
| `--since 1m` | 최근 1분 이후 로그부터 보여 준다. |

`Started ...`가 보이면 `Ctrl+C`로 로그 보기만 종료한다.

**입력·실행 — 터미널 B**

```bash
# 세 서비스가 모두 실행 중이고 Nginx만 호스트 포트를 공개했는지 확인한다.
docker compose ps
# Nginx를 통해 요청해 PetClinic 응답이 정상인지 확인한다.
curl -i 'http://localhost:8080/?study=step2'
```

| 전체 명령·핵심 구성 | 확인하는 것 |
| --- | --- |
| `docker compose ps` | app·postgres·nginx가 모두 실행 중인지 확인한다. |
| `curl` | Nginx의 공개 주소로 HTTP 요청을 보낸다. |
| `-i` | 응답 본문뿐 아니라 HTTP 상태와 응답 헤더도 함께 출력한다. |
| `?study=step2` | Nginx 로그와 Tomcat 로그에서 같은 요청을 찾기 위한 시험 표식이다. |

**결과·로그 판정**

| 확인 항목 | 정상 결과 |
| --- | --- |
| app 로그 | `Started ...` |
| `docker compose ps` | 세 서비스 실행 중, Nginx에만 `8080->80` 표시 |
| `curl -i` | `HTTP/1.1 200` |
| PC 브라우저 | `http://localhost:8080`에서 PetClinic 화면 표시 |

**짧은 설명**

1단계에서 저장한 포트·네트워크 구성을 컨테이너에 반영한다. 호스트의 `localhost:8080`은 Nginx로 연결되고, `app:8080`과 `postgres:5432`는 컨테이너 사이에서만 사용한다.

### 2-2. Nginx가 app으로 전달했는지 로그로 확인

> **이 단계에서 검사하는 것:** 같은 요청이 Nginx access log와 Tomcat access log에 모두 남는지 비교해 Nginx가 app까지 전달했음을 증명한다.

**입력·실행 — 터미널 B**

```bash
# 최근 Nginx 로그에서 2단계 시험 요청만 찾아 app 전달 주소를 확인한다.
docker compose logs --since 2m --no-log-prefix nginx | \
  grep 'study=step2'

# app 컨테이너 안의 Tomcat access log에서 같은 요청을 찾는다.
docker compose exec -T app sh -c \
  "grep -h 'study=step2' /tmp/tomcat/access/access_log*.log"
```

**명령 한눈에 보기**

| 핵심 구성 | 이 명령에서 하는 역할 |
| --- | --- |
| `logs --since 2m nginx` | 최근 2분의 Nginx 로그만 가져온다. |
| `--no-log-prefix` | Compose가 앞에 붙이는 서비스 이름을 빼 로그 본문을 쉽게 검색한다. |
| `grep 'study=step2'` | 이번 시험 요청이 들어간 줄만 고른다. |
| `exec -T app` | app 컨테이너 안에서 명령을 실행한다. |
| `grep ... /tmp/tomcat/access/...` | 컨테이너 안의 Tomcat access log에서 같은 시험 요청을 찾는다. |

**결과·로그 판정**

Nginx 로그에 `status=200`, `upstream="...:8080"`이 있고 Tomcat 로그에도 `study=step2`가 있으면 정상이다.

**짧은 설명**

같은 시험 표식을 두 로그에서 찾으면 `proxy_pass http://app:8080`을 통해 요청이 Nginx에서 PetClinic까지 전달됐음을 확인할 수 있다.

### 2-3. app·PostgreSQL 비공개 판정

> **이 단계에서 검사하는 것:** app과 PostgreSQL은 실행 중이지만 호스트 포트가 게시되지 않아 외부에서 직접 접근할 수 없는 상태인지 확인한다.

**입력·실행 — 터미널 B**

```bash
# app과 postgres가 꺼진 것이 아니라 실행 중인지, nginx만 호스트 포트를 공개했는지 확인한다.
docker compose ps
# app의 내부 8080에 연결된 호스트 포트가 있는지 조회한다.
docker compose port app 8080 2>/dev/null || true
# PostgreSQL 내부 5432에 연결된 호스트 포트가 있는지 조회한다.
docker compose port postgres 5432 2>/dev/null || true
```

**명령 한눈에 보기**

| 전체 명령·핵심 구성 | 확인하는 것 |
| --- | --- |
| `docker compose ps` | app·postgres가 실행 중이고 nginx에만 `0.0.0.0:8080->80/tcp`가 있는지 확인한다. |
| `port app 8080` | app 내부 8080과 연결된 호스트 포트가 있는지 확인한다. |
| `port postgres 5432` | PostgreSQL 내부 5432와 연결된 호스트 포트가 있는지 확인한다. |
| `2>/dev/null \|\| true` | 비공개 포트를 조회했을 때 나올 수 있는 오류 문구는 숨기되, 다음 확인을 계속 진행한다. |

**결과·로그 판정**

```text
nginx      0.0.0.0:8080->80/tcp
app        8080/tcp
postgres   5432/tcp
```

두 `docker compose port` 명령의 결과는 Compose 버전에 따라 **출력이 없거나 `:0`**일 수 있으며, 둘 다 호스트에 실제 게시된 포트가 없다는 뜻이다. `0.0.0.0:포트`, `127.0.0.1:포트`, `[::]:포트`처럼 실제 주소와 0이 아닌 포트가 나오면 해당 서비스의 `ports:`를 제거한다. 최종 판정은 바로 위 `docker compose ps`에서 Nginx에만 `8080->80`이 표시되는지도 함께 본다.

**짧은 설명**

`localhost:8080`은 이미 Nginx에 연결됐으므로 같은 주소로 app 직접 접근을 시험할 수 없다. PostgreSQL도 HTTP 서버가 아니므로 `curl`이 아니라 Compose의 실제 포트 게시 결과로 비공개 여부를 판정한다.

### 2-4. 선택: 같은 Wi-Fi의 휴대폰에서 Nginx 접속

> **이 단계에서 검사하는 것:** 같은 네트워크의 다른 기기에서도 Nginx의 공개 포트로 접속할 수 있고, 요청이 Nginx 로그에 남는지 선택적으로 확인한다.

**입력·실행 — 휴대폰 브라우저**

```text
http://<노트북-LAN-IP>:8080/?study=step2-external
```

**입력·실행 — 터미널 B**

```bash
# 최근 Nginx 로그에서 외부 장치가 보낸 시험 요청만 찾아 실제 도달 여부를 확인한다.
docker compose logs --since 5m --no-log-prefix nginx | \
  grep 'study=step2-external'
```

**명령 한눈에 보기**

| 핵심 구성 | 이 명령에서 하는 역할 |
| --- | --- |
| `logs --since 5m nginx` | 휴대폰 시험을 한 최근 5분의 Nginx 로그만 본다. |
| `grep 'study=step2-external'` | 휴대폰 주소에 붙인 시험 요청만 고른다. |

**결과·로그 판정**

휴대폰에 PetClinic 화면이 열리고 Nginx 로그에 `study=step2-external`, `status=200`, app upstream이 있으면 정상이다.

**짧은 설명**

환경세팅에서 확인한 노트북 LAN IP로 Nginx의 외부 장치 접근만 시험한다. app·PostgreSQL 비공개 여부는 앞의 `docker compose port` 결과로 판정한다.

---

## 3. 정적 파일 캐시 효과 비교하기

### 3-1. 실제 캐시 대상 이미지 확인

> **이 단계에서 검사하는 것:** 동적 화면이 아니라 실제 PNG 정적 파일을 선택하고, 요청 결과가 `200`과 이미지 형식인지 확인한다.

**입력·실행 — 터미널 B**

```bash
# PetClinic 소스에서 캐시 시험에 사용할 실제 pets.png 파일 위치를 찾는다.
find src/main/resources -type f -name 'pets.png'
# 실행 중인 PetClinic의 이미지 URL이 200과 image/png로 응답하는지 확인한다.
curl -sS -D - -o /dev/null \
  http://localhost:8080/resources/images/pets.png
```

**명령 한눈에 보기**

| 핵심 구성 | 이 명령에서 하는 역할 |
| --- | --- |
| `find src/main/resources` | PetClinic의 정적 파일 폴더 아래를 검색한다. |
| `-type f -name 'pets.png'` | 폴더가 아닌 파일 중 이름이 `pets.png`인 것만 찾는다. |
| `curl` | 지정한 URL로 HTTP 요청을 보낸다. |
| `-s` | 진행률과 일반 안내 문구를 숨긴다. |
| `-S` | `-s`를 사용해도 연결 실패 같은 오류 메시지는 표시한다. `-sS`는 두 옵션을 함께 쓴 표기다. |
| `-D -` | `-D`는 응답 헤더를 따로 출력하고, 뒤의 `-`는 그 헤더를 터미널 표준 출력으로 보낸다는 뜻이다. |
| `-o /dev/null` | 이미지 본문은 버리고 헤더만 확인한다. |

**결과·로그 판정**

`src/main/resources/static/resources/images/pets.png`가 검색되고 HTTP `200`과 `Content-Type: image/png`가 나오면 정상이다. 파일이 없다면 브라우저 Network 탭에서 실제 정적 이미지 URL을 선택한다.

**짧은 설명**

동적 화면이 아니라 내용이 잘 바뀌지 않는 실제 이미지로 캐시를 비교한다. PC 브라우저에서도 해당 URL을 열어 이미지가 보이는지 확인한다.

### 3-2. 캐시 설정과 BYPASS·MISS·HIT 확인

> **이 단계에서 검사하는 것:** 캐시 우회 요청은 `BYPASS`, 새 URL의 첫 요청은 `MISS`, 같은 URL의 재요청은 `HIT`가 되는지 확인한다.

**입력·수정 — 터미널 B**

```bash
# 캐시 저장 공간 정의를 추가하기 위해 Nginx 관찰 설정 파일을 연다.
nano nginx/conf.d/00-observability.conf
```

nano 화면의 맨 아래에 다음 설정을 추가하고 저장·종료한다.

```nginx
proxy_cache_path /var/cache/nginx levels=1:2 keys_zone=petclinic_cache:10m
                 inactive=10m max_size=100m use_temp_path=off;
```

**설정 한눈에 보기**

| 설정 | 이 실습에서 하는 역할 |
| --- | --- |
| `proxy_cache_path` | Nginx 캐시 파일을 저장할 위치를 정한다. |
| `keys_zone` | 캐시 목록을 기억할 메모리 영역의 이름과 크기를 정한다. |
| `inactive` | 10분 동안 사용하지 않은 캐시를 정리한다. |
| `max_size` | 디스크 캐시의 최대 크기를 100MB로 제한한다. |

**입력·수정 — 터미널 B · 웹 서버 설정 파일**

```bash
# 정적 파일 요청에 캐시를 적용하기 위해 Nginx 웹 서버 설정 파일을 연다.
nano nginx/conf.d/default.conf
```

nano 화면에서 `server {}` 안의 기존 `location /` 바로 위에 다음 블록을 추가하고 저장·종료한다.

```nginx
location ~* \.(?:css|js|png|jpe?g|gif|svg|ico)$ {
  proxy_pass http://app:8080;
  proxy_set_header Host $host;
  proxy_set_header X-Real-IP $remote_addr;
  proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
  proxy_set_header X-Forwarded-Proto $scheme;
  proxy_cache petclinic_cache;
  proxy_cache_valid 200 5m;
  proxy_cache_methods GET HEAD;
  proxy_cache_bypass $arg_bypass_cache;
  proxy_no_cache $arg_bypass_cache;
  add_header X-Cache-Status $upstream_cache_status always;
}
```

**정적 파일 캐시 설정 한눈에 보기**

| 설정 | 이 실습에서 하는 역할 |
| --- | --- |
| `location ~* ...` | CSS·JavaScript·이미지 확장자 요청만 선택한다. |
| `proxy_cache` | 앞에서 만든 `petclinic_cache`를 사용한다. |
| `proxy_cache_valid` | 상태 코드 200 응답을 5분 동안 캐시한다. |
| `proxy_cache_methods` | GET·HEAD 요청만 캐시 대상으로 삼는다. |
| `proxy_cache_bypass` | URL에 `bypass_cache` 값이 있으면 저장된 캐시를 사용하지 않는다. |
| `proxy_no_cache` | 우회 요청으로 받은 응답은 새 캐시로 저장하지 않는다. |
| `X-Cache-Status` | 응답 헤더에서 `BYPASS`, `MISS`, `HIT`를 확인하게 한다. |

**입력·실행 — 터미널 B**

```bash
# 수정한 캐시 설정의 Nginx 문법이 올바른지 검사한다.
docker compose exec nginx nginx -t
# 문법 검사가 성공한 설정을 실행 중인 Nginx에 반영한다.
docker compose exec nginx nginx -s reload
```

| 전체 명령·핵심 구성 | 하는 일 |
| --- | --- |
| `nginx -t` | 저장한 Nginx 설정의 문법을 검사한다. |
| `nginx -s reload` | 실행 중인 Nginx가 새 설정을 다시 읽게 한다. |

**입력·실행 — 터미널 B**

```bash
# 이전 캐시와 겹치지 않도록 이번 시험만의 고유 ID를 만든다.
STUDY_ID="$(date +%s)-$$"
# 캐시 시험에 사용할 실제 이미지 경로를 변수에 저장한다.
IMAGE_PATH="resources/images/pets.png"
# 같은 URL을 반복 사용할 수 있도록 시험용 전체 주소를 만든다.
ASSET="http://localhost:8080/$IMAGE_PATH?study=$STUDY_ID"

# 캐시를 우회해 실제 이미지 형식·크기·단일 응답 시간을 확인한다.
curl -sS -D - -o /tmp/petclinic-cache-image \
  -w 'type=%{content_type} bytes=%{size_download} dns=%{time_namelookup}s tcp=%{time_connect}s tls=%{time_appconnect}s first_byte=%{time_starttransfer}s total=%{time_total}s\n' \
  "$ASSET&bypass_cache=1"

# 첫 일반 요청에서 실제 X-Cache-Status가 MISS인지 확인한다.
curl -sS -D - -o /dev/null \
  -w '일반 요청 1 dns=%{time_namelookup}s tcp=%{time_connect}s tls=%{time_appconnect}s first_byte=%{time_starttransfer}s total=%{time_total}s\n' "$ASSET" | \
  grep -iE 'HTTP/|X-Cache-Status|dns='
# 같은 URL을 다시 요청해 실제 X-Cache-Status가 HIT인지 확인한다.
curl -sS -D - -o /dev/null \
  -w '일반 요청 2 dns=%{time_namelookup}s tcp=%{time_connect}s tls=%{time_appconnect}s first_byte=%{time_starttransfer}s total=%{time_total}s\n' "$ASSET" | \
  grep -iE 'HTTP/|X-Cache-Status|dns='
```

**명령 한눈에 보기**

| 핵심 구성 | 이 명령에서 하는 역할 |
| --- | --- |
| `STUDY_ID="$(date +%s)-$$"` | 현재 시각과 현재 셸 번호를 합쳐, 예전 시험 캐시와 겹치지 않을 시험 ID를 만든다. |
| `IMAGE_PATH`·`ASSET` | 반복해서 사용할 이미지 경로와 전체 URL을 저장한다. |
| `curl` | `ASSET` 변수에 저장된 이미지 URL로 HTTP 요청을 보낸다. |
| `-s` | 다운로드 진행률과 일반 안내 문구를 숨긴다. |
| `-S` | `-s` 상태에서도 오류 메시지는 표시한다. `-sS`는 두 옵션의 결합이다. |
| `-D -` | 응답 헤더를 터미널에 출력해 `HTTP` 상태와 `X-Cache-Status`를 볼 수 있게 한다. |
| `-o /tmp/petclinic-cache-image` | 첫 요청의 이미지 본문을 해당 임시 파일에 저장한다. |
| `-o /dev/null` | 뒤의 두 요청에서는 이미지 본문을 버리고 헤더와 측정값만 확인한다. |
| `-w '...'` | 요청이 끝난 뒤 작은따옴표 안의 형식대로 측정 결과를 출력한다. |
| `%{content_type}` | 서버가 응답한 파일 형식이다. 이 실습에서는 `image/png`가 기대값이다. |
| `%{size_download}` | 실제로 내려받은 응답 본문의 바이트 수다. |
| `%{time_namelookup}` | curl 시작부터 `localhost` 이름을 주소로 확인할 때까지의 누적 시간이다. |
| `%{time_connect}` | curl 시작부터 Nginx 공개 포트와 TCP 연결을 완료할 때까지의 누적 시간이다. DNS 이후 TCP 연결 구간은 대략 `time_connect - time_namelookup`이다. |
| `%{time_appconnect}` | curl 시작부터 HTTPS의 TLS 연결을 완료할 때까지의 누적 시간이다. 현재 실습은 HTTP이므로 보통 `0`이다. |
| `%{time_starttransfer}` | curl 시작부터 Nginx 응답의 첫 바이트를 받을 때까지의 누적 시간이다. Nginx 처리와 MISS·BYPASS의 app 처리시간이 포함된다. |
| `%{time_total}` | curl 시작부터 응답 본문을 모두 받을 때까지의 전체 시간이다. 본문 수신 구간은 대략 `time_total - time_starttransfer`다. |
| `\n` | 측정 결과 출력 후 줄을 바꾼다. |
| 줄 끝의 `\` | 명령이 다음 줄에 계속된다는 POSIX 셸 표기다. Enter를 눌러도 다음 줄을 이어서 한 명령으로 실행한다. |
| `"$ASSET"` | 앞에서 만든 이미지 시험 URL을 변수에서 꺼내 사용한다. |
| `&bypass_cache=1` | 기존 `study=...` 쿼리 뒤에 두 번째 쿼리 값을 붙여 Nginx 캐시를 일부러 우회한다. URL 전체를 큰따옴표로 감싸므로 `&`가 셸의 백그라운드 기호로 해석되지 않는다. |
| `\| grep -iE ...` | curl 출력 중 HTTP 상태·캐시 상태·시간에 해당하는 줄만 고른다. `-i`는 대소문자를 무시하고 `-E`는 여러 검색 조건을 사용한다. |

**결과·로그 판정**

실제 판정은 curl에 붙인 설명 문구가 아니라 `X-Cache-Status` 응답 헤더로 한다. 다음 순서가 나와야 한다.

```text
X-Cache-Status: BYPASS
X-Cache-Status: MISS
X-Cache-Status: HIT
```

`MISS`만 반복되면 정상으로 넘기지 말고 04-트러블슈팅의 캐시 항목을 확인한다.

**짧은 설명**

요청은 로컬의 `curl → localhost:8080 → Nginx`로 들어간다. `HIT`이면 Nginx 캐시에서 끝나고, `MISS·BYPASS`이면 Docker 내부망의 `app:8080`까지 전달된다. `proxy_cache_path`는 캐시 저장소를 만들고 정적 파일 `location`의 `proxy_cache`가 그 저장소를 적용한다. 이 과정은 한 노트북 안에서 일어나며 인터넷은 지나지 않는다.

### 3-3. 단일 시간·반복 시간·app 전달 횟수 비교

> **이 단계에서 검사하는 것:** 캐시 사용 전후의 응답 시간뿐 아니라 Tomcat 도달 횟수까지 비교해 Nginx가 app의 요청 처리를 실제로 줄였는지 확인한다.

#### 3-3-1. 순차 요청 300개의 전체 시간

**입력·실행 — 터미널 B · 앞 단계의 `ASSET` 사용**

```bash
# 3-2에서 만든 시험 ID와 이미지 주소가 현재 터미널에 남아 있는지 확인한다.
: "${STUDY_ID:?먼저 3-2를 실행해 STUDY_ID를 만드세요}"
: "${ASSET:?먼저 3-2를 실행해 ASSET을 만드세요}"
# 아래의 측정용 하위 셸에서도 같은 이미지 주소를 사용할 수 있게 전달한다.
export ASSET

# 캐시를 300번 우회했을 때 순차 요청 전체 시간을 측정한다.
time sh -c '
  awk "BEGIN {for(i=1;i<=300;i++) print i}" | \
    xargs -P 1 -I{} curl -sS -o /dev/null \
      -H "X-Study-Run: {}" "$ASSET&bypass_cache=1"
'

# 같은 이미지를 캐시로 300번 처리했을 때 순차 요청 전체 시간을 측정한다.
time sh -c '
  awk "BEGIN {for(i=1;i<=300;i++) print i}" | \
    xargs -P 1 -I{} curl -sS -o /dev/null \
      -H "X-Study-Run: {}" "$ASSET"
'
```

**명령 한눈에 보기**

두 명령은 같은 이미지에 HTTP 요청을 **순서대로 300번** 보내고, 300회가 모두 끝날 때까지 걸린 시간을 비교한다. 첫 번째만 캐시를 우회하고 두 번째는 캐시를 사용한다.

```text
awk가 숫자 300개 생성
→ xargs가 숫자 하나마다 curl 한 번 실행
→ -P 1이므로 앞 요청이 끝난 뒤 다음 요청 실행
→ 300회가 끝나면 time이 전체 시간 출력
```

| 핵심 구성 | 이 명령에서 하는 역할 |
| --- | --- |
| `: "${변수:?...}"` | 3-2의 변수가 없으면 잘못된 주소로 시험하지 않고 이유를 바로 표시한다. |
| `export ASSET` | 현재 터미널의 이미지 주소를 `sh -c`로 실행하는 측정용 하위 셸에도 전달한다. |
| `time sh -c '...'` | 작은따옴표 안의 파이프라인 전체를 하나의 하위 셸에서 실행하고, 숫자 생성부터 마지막 curl 종료까지의 시간을 측정한다. |
| `awk 'BEGIN {...300...}'` | `1`부터 `300`까지 출력해 curl을 실행할 작업 300개를 만든다. |
| `\|` | 왼쪽의 숫자 300개를 오른쪽 `xargs`에 전달한다. |
| `xargs -P 1 -I{}` | 입력 한 줄마다 뒤의 curl을 한 번 실행한다. `-P 1`이므로 동시에 하나만 실행하며 `{}`는 작업 번호로 바뀐다. |
| `-H 'X-Study-Run: {}'` | 각 curl에 작업 번호 헤더를 붙여 `{}`를 사용한다. 캐시 키인 URL은 바꾸지 않는다. |
| `curl -sS -o /dev/null "$ASSET"` | 앞 단계에서 저장한 이미지 URL을 요청하되 진행률과 이미지 본문은 버리고 오류만 표시한다. |
| `&bypass_cache=1` | 첫 실험의 300개 요청이 모두 Nginx 캐시를 우회하게 한다. |

| time 결과 | 의미 |
| --- | --- |
| `real` | 시작부터 300개 요청이 모두 끝날 때까지 사람이 실제로 기다린 전체 시간이다. 두 실험은 이 값을 비교한다. |
| `user` | 명령 실행 중 사용자 영역에서 CPU가 일한 누적 시간이다. HTTP 응답시간 자체가 아니다. |
| `sys` | 프로세스·네트워크·파일 처리 등을 위해 운영체제 커널이 일한 누적 시간이다. HTTP 응답시간 자체가 아니다. |

**결과·로그 판정**

두 명령의 `real`을 비교한다. 첫 번째는 300회 모두 app까지 전달되고, 두 번째는 대부분 Nginx `HIT`이므로 일반적으로 두 번째가 더 짧다.

#### 3-3-2. 선택: 동시 요청 200개의 전체 시간

**선택 입력·실행 — 터미널 B**

```bash
# 캐시를 우회하는 요청 200개를 최대 20개씩 동시에 보내 전체 시간을 측정한다.
time sh -c '
  awk "BEGIN {for(i=1;i<=200;i++) print i}" | \
    xargs -P 20 -I{} curl -sS -o /dev/null \
      -H "X-Study-Run: {}" "$ASSET&bypass_cache=1"
'

# 캐시를 사용하는 요청 200개를 최대 20개씩 동시에 보내 전체 시간을 측정한다.
time sh -c '
  awk "BEGIN {for(i=1;i<=200;i++) print i}" | \
    xargs -P 20 -I{} curl -sS -o /dev/null \
      -H "X-Study-Run: {}" "$ASSET"
'
```

**병렬 명령 한눈에 보기**

두 명령은 요청을 **총 200번** 보내되 최대 20개를 동시에 실행한다. `200 × 20`이 아니며, 200개 작업을 20개씩 나누어 처리한다. 나머지 구조는 앞의 순차 명령과 같고 `xargs -P 1`만 `xargs -P 20`으로 바뀐다.

```text
전체 요청 200개
→ 동시에 최대 20개 실행
→ 한 요청이 끝나면 다음 대기 요청 실행
→ 200개가 모두 끝나면 time이 전체 시간 출력
```

#### 3-3-3. 로그로 app 전달 횟수 확인

**입력·실행 — 터미널 B**

```bash
# 이번 시험 ID가 붙은 Nginx 로그만 찾아 BYPASS·MISS·HIT 처리 결과를 확인한다.
# 캐시 비교·반복 요청을 모두 끝낸 뒤에도 찾을 수 있도록 최근 1시간을 조회한다.
docker compose logs --since 1h --no-log-prefix nginx | \
  grep "study=$STUDY_ID" | grep -E 'cache=(BYPASS|MISS|HIT)'

# app 컨테이너 안에서 Tomcat access log의 최근 20줄을 확인한다.
docker compose exec -T app sh -c \
  'tail -n 20 /tmp/tomcat/access/access_log*.log'

# 이번 시험 요청 중 PetClinic까지 전달된 전체 횟수를 센다.
docker compose exec -T app sh -c \
  "grep -h 'study=$STUDY_ID' /tmp/tomcat/access/access_log*.log | wc -l"

# bypass_cache=1이 붙어 PetClinic까지 전달된 요청 횟수를 센다.
docker compose exec -T app sh -c \
  "grep -h 'study=$STUDY_ID.*bypass_cache=1' /tmp/tomcat/access/access_log*.log | wc -l"

# 일반 URL 중 PetClinic까지 전달된 요청 횟수를 세어 HIT가 앱에 가지 않았는지 확인한다.
docker compose exec -T app sh -c \
  "grep -h 'study=$STUDY_ID' /tmp/tomcat/access/access_log*.log | grep -v 'bypass_cache=1' | wc -l"
```

| 핵심 구성 | 이 명령에서 하는 역할 |
| --- | --- |
| Nginx `logs` + `grep` | 이번 시험의 `BYPASS`, `MISS`, `HIT` 로그만 찾는다. |
| `tail -n 20` | Tomcat 로그의 마지막 20줄을 먼저 눈으로 확인한다. |
| `wc -l` | 검색된 요청 줄 수를 숫자로 센다. |
| `grep -v 'bypass_cache=1'` | 우회 요청을 제외하고 캐시 사용 URL만 센다. |

**결과·로그 판정**

`BYPASS·MISS`에는 `upstream=...`와 Tomcat 로그가 남고, `HIT`에는 upstream이 `-`이며 Tomcat 요청 수가 늘지 않아야 한다. 캐시 사용 시간이 비슷하더라도 Tomcat 전달 횟수가 확실히 작으면 캐시는 작동한 것이다.

| 실험 | 보낸 요청 | PetClinic 전달 |
| --- | ---: | ---: |
| 처음 이미지 확인 `BYPASS` | 1 | 1 |
| 첫 일반 요청 `MISS` | 1 | 1 |
| 두 번째 일반 요청 `HIT` | 1 | 0 |
| 순차 `BYPASS` 시험 | 300 | 300 |
| 순차 캐시 시험 | 300 | 0 |
| 동시 `BYPASS` 시험 | 200 | 200 |
| 동시 캐시 시험 | 200 | 0 |

**짧은 설명**

`real`은 첫 명령 시작부터 마지막 curl 종료까지의 전체 대기시간이며 프로세스 실행·로컬 TCP·Nginx·app·응답 수신을 모두 포함한다. `user`는 프로그램 CPU 시간, `sys`는 커널 CPU 시간이다. 순차 실험은 누적 시간 차이를, 선택 병렬 실험은 여러 요청이 몰렸을 때의 처리시간을 비교한다.

---

## 4. Nginx 앞단에서 요청 제한·보호하기

### 4-1. 요청 속도 제한과 429

> **이 단계에서 검사하는 것:** 짧은 시간에 많은 요청을 보내면 Nginx가 일부를 `429`로 거부하고, 거부된 요청은 app까지 전달하지 않는지 확인한다.

**입력·수정 — 터미널 B**

```bash
# 요청 제한 기준을 추가하기 위해 Nginx 관찰 설정 파일을 연다.
nano nginx/conf.d/00-observability.conf
```

nano 화면의 맨 아래에 다음 설정을 추가하고 저장·종료한다.

```nginx
limit_req_zone $binary_remote_addr zone=per_ip_rate:10m rate=5r/s;
```

**설정 한눈에 보기**

| 설정 | 이 실습에서 하는 역할 |
| --- | --- |
| `limit_req_zone` | 클라이언트별 요청 속도를 기억할 공통 영역을 만든다. |
| `$binary_remote_addr` | 요청을 보낸 IP 주소를 기준으로 구분한다. |
| `zone=per_ip_rate:10m` | 영역 이름을 `per_ip_rate`, 메모리 크기를 10MB로 정한다. |
| `rate=5r/s` | IP 하나당 평균 초당 5개 요청을 기준으로 삼는다. |

`nginx/conf.d/default.conf`의 `server`에는 거절 상태와 로그 수준을, 일반 `location /`에는 실제 제한 규칙을 추가한다.

**입력·수정 — 터미널 B**

```bash
# 실제 요청 경로에 요청 제한을 적용하기 위해 Nginx 웹 서버 설정 파일을 연다.
nano nginx/conf.d/default.conf
```

nano 화면에서 아래 주석에 표시된 두 위치에 각각 설정을 추가하고 저장·종료한다. `limit_req_status`와 `limit_req_log_level`은 **`server {` 시작 뒤, 첫 번째 `location`이 나오기 전**에 둔다. 정적 파일용 `location ~*` 안에 넣으면 일반 `/` 요청에는 적용되지 않는다.

```nginx
server {
  listen 80;
  access_log /dev/stdout study;

  # 여기에 추가: server 전체에 적용
  limit_req_status 429;
  limit_req_log_level notice;

  # 기존 정적 파일 location은 그대로 유지
  location ~* \.(?:css|js|png|jpe?g|gif|svg|ico)$ {
    # 기존 캐시 설정 ...
  }

  location / {
    # 기존 proxy 설정 ...

    # 여기에 추가: 일반 PetClinic 요청에 속도 제한 적용
    limit_req zone=per_ip_rate burst=10 nodelay;
  }
}
```

| 설정 | 이 실습에서 하는 역할 |
| --- | --- |
| `limit_req_status 429` | 제한된 요청에 429 상태 코드를 반환한다. |
| `limit_req_log_level notice` | 제한 발생을 Nginx notice 로그에 남긴다. |
| `limit_req` | 일반 PetClinic 요청에 `per_ip_rate` 제한을 적용한다. |
| `burst=10` | 순간적으로 기준을 넘는 요청을 10개까지 추가로 허용한다. |
| `nodelay` | 허용된 burst 요청을 지연시키지 않고 바로 처리한다. |

**입력·실행 — 터미널 B**

```bash
# 수정한 요청 제한 설정의 Nginx 문법이 올바른지 검사한다.
docker compose exec nginx nginx -t
# 문법 검사가 성공한 요청 제한 설정을 실행 중인 Nginx에 반영한다.
docker compose exec nginx nginx -s reload

# 실행 중인 Nginx에 요청 제한의 기준과 실제 적용 규칙이 모두 있는지 확인한다.
docker compose exec nginx nginx -T 2>&1 | \
  grep -n -C 2 -E 'limit_req_zone|limit_req '

# 요청 한 건은 제한되지 않고 200으로 처리되는지 먼저 확인한다.
curl -sS -o /dev/null -w '%{http_code}\n' \
  'http://localhost:8080/?study=step4-ok'

# 이전 시험 로그와 섞이지 않을 이번 요청 제한 시험 ID를 만든다.
RATE_TEST_ID="step4-rate-$(date +%s)-$$"
# 요청 50개를 최대 20개씩 동시에 보내 200과 429의 개수를 확인한다.
awk 'BEGIN {for(i=1;i<=50;i++) print i}' | \
  xargs -P 20 -I{} curl -sS -o /dev/null -w '%{http_code}\n' \
    "http://localhost:8080/?study=$RATE_TEST_ID&request={}" | \
  sort | uniq -c

# 이번 시험 ID의 Nginx 로그에서 요청 제한으로 거부된 429 요청만 찾는다.
docker compose logs --since 2m --no-log-prefix nginx | \
  grep "study=$RATE_TEST_ID" | grep -E 'rate_limit=REJECTED|status=429'

# 같은 시험 요청이 PetClinic까지 몇 건 전달됐는지 Tomcat 로그에서 센다.
docker compose exec -T app sh -c \
  "grep -h 'study=$RATE_TEST_ID' /tmp/tomcat/access/access_log*.log | wc -l"
```

**요청 50개 생성 명령을 왼쪽부터 읽기**

```text
awk가 1~50 생성
→ xargs가 숫자 하나당 curl 한 번 실행
→ curl이 Nginx에 요청하고 상태 코드만 출력
→ sort가 같은 상태 코드를 모음
→ uniq -c가 200과 429의 개수를 셈
```

| 명령 구성 | 뜻 |
| --- | --- |
| `awk` | 입력을 처리하거나 값을 직접 만들어 출력하는 프로그램이다. 여기서는 `BEGIN` 안에서 숫자를 만든다. |
| `'BEGIN { ... }'` | 입력 파일을 읽기 전에 중괄호 안의 작업을 한 번 실행한다. 이번 명령에는 입력 파일이 없으므로 이 부분만 실행된다. |
| `for(i=1; i<=50; i++)` | 변수 `i`를 1로 시작하고, 50 이하인 동안 반복하며, 한 번 반복할 때마다 1씩 증가시킨다. |
| `print i` | 반복할 때마다 현재 숫자를 한 줄씩 출력한다. 결과적으로 1부터 50까지 50줄이 만들어진다. |
| 첫 번째 `\|` | `awk`가 만든 숫자 50줄을 `xargs`의 입력으로 전달한다. |
| `xargs` | 입력으로 받은 각 줄을 뒤에 적힌 명령의 인자로 바꾸어 명령을 반복 실행한다. |
| `-P 20` | curl 작업을 한꺼번에 최대 20개까지 실행한다. 총 요청은 50개이고, 그중 동시에 실행되는 최대 개수가 20개다. |
| `-I{}` | 입력받은 숫자를 뒤 명령의 `{}` 자리에 넣는다. 1번째 작업은 `request=1`, 50번째 작업은 `request=50`이 된다. |
| `curl` | 만들어진 URL로 HTTP 요청을 보낸다. 숫자 50개가 들어오므로 총 50번 실행된다. |
| `-s` | 진행률과 일반 안내 문구를 숨긴다. |
| `-S` | `-s`를 사용해도 실제 요청 오류는 표시한다. `-sS`는 두 옵션을 함께 쓴 표기다. |
| `-o /dev/null` | 응답 본문은 필요하지 않으므로 버린다. |
| `-w '%{http_code}\n'` | 각 요청이 끝나면 응답 상태 코드만 한 줄로 출력한다. |
| `study=$RATE_TEST_ID` | 이번 시험 요청을 로그에서 구분하기 위한 공통 ID다. |
| `request={}` | 각 요청에 1~50 번호를 붙여 서로 다른 요청임을 구분한다. |
| 두 번째 `\|` | curl 50개가 출력한 상태 코드를 `sort`로 전달한다. |
| `sort` | 같은 상태 코드가 이웃하도록 숫자를 정렬한다. |
| `uniq -c` | 연속된 같은 상태 코드가 몇 줄인지 센다. 예를 들어 `11 200`, `39 429`처럼 출력한다. |

**전체 실행 흐름과 나머지 확인 명령**

이 코드 블록은 다음 다섯 작업을 위에서 아래로 수행한다.

1. 새 설정의 문법을 검사하고 Nginx에 반영한다.
2. 실행 중인 설정에 제한 기준과 적용 규칙이 모두 있는지 확인한다.
3. 요청 한 건으로 기본 `200` 응답을 확인한다.
4. 요청 50개를 최대 20개씩 동시에 보내 `200`과 `429` 개수를 센다.
5. Nginx와 Tomcat 로그를 비교해 429 요청이 app에 전달되지 않았는지 확인한다.

| 처음 볼 만한 구성 | 이 명령에서 하는 역할 |
| --- | --- |
| `nginx -T 2>&1 \| grep ...` | Nginx가 실제로 읽은 전체 설정을 출력한 뒤 요청 제한 관련 줄만 고른다. |
| `RATE_TEST_ID="..."` | 이번 시험 로그만 찾을 수 있도록 고유한 이름을 저장한다. |
| Nginx 로그 검색 | 거부된 요청의 `status=429`와 `rate_limit=REJECTED`를 찾는다. |
| Tomcat `grep ... \| wc -l` | 50개 중 app까지 실제로 도착한 요청 수를 센다. |

**결과·로그 판정**

| 실행한 명령 | 이 명령이 하는 일 | 정상이라면 보일 결과 |
| --- | --- | --- |
| `RATE_TEST_ID=...` | 이번 시험 전용 이름을 변수에 저장한다. | 성공해도 화면 출력이 없다. |
| `awk ... \| xargs ... \| sort \| uniq -c` | 총 50개 요청을 최대 20개씩 동시에 보내고, 응답 상태 코드별 개수를 센다. | `200`과 `429`가 함께 나온다. 예: `11 200`, `39 429` |
| Nginx `logs ... grep` | 이번 시험 ID의 Nginx 처리 로그만 찾아 제한 여부를 본다. | 허용 요청은 `rate_limit=PASSED status=200`, 거부 요청은 `rate_limit=REJECTED status=429` |
| app `grep ... wc -l` | PetClinic까지 실제로 도착한 요청 수를 센다. | 숫자가 50보다 작다. 429 요청은 app까지 전달되지 않는다. |

`50 200`만 나오면 제한이 작동하지 않은 것이다. 특히 Nginx 로그에 `rate_limit=-`가 보이면, Nginx가 그 요청을 제한 규칙이 적용된 `location`에서 처리하지 않았다는 신호다. 이 경우 04-트러블슈팅의 요청 제한 항목에서 **실제 요청이 선택한 server·location**을 먼저 확인한다.

`sort | uniq -c` 결과는 환경마다 개수가 달라도 `200`과 `429`가 함께 나오면 정상이다. 예를 들면 `11 200`, `39 429`처럼 표시된다.

**짧은 설명**

`00-observability.conf`에는 IP별 요청 속도 기준을 만들고, `default.conf`에는 그 기준을 일반 PetClinic 경로에 적용한다. Nginx에서 거부한 429 요청은 app까지 전달되지 않는다.

### 4-2. 요청 본문 크기 제한과 413

> **이 단계에서 검사하는 것:** 설정한 1KiB보다 큰 2KiB 요청 본문을 Nginx가 `413`으로 차단하고, Tomcat에는 같은 요청이 남지 않는지 확인한다.

**입력·수정 — 터미널 B**

```bash
# 요청 본문 크기 제한을 추가하기 위해 Nginx 웹 서버 설정 파일을 연다.
nano nginx/conf.d/default.conf
```

nano 화면에서 `client_max_body_size`를 먼저 검색한다. 이미 `client_max_body_size 1m;` 같은 줄이 있다면 새 줄을 추가하지 말고 그 줄을 `client_max_body_size 1k;`로 **교체**한다. 해당 설정이 없을 때만 `access_log`와 4-1의 요청 제한 설정 다음, 첫 번째 `location`보다 앞에 한 줄 추가하고 저장·종료한다. `server {}` 안에는 이 설정이 정확히 한 번만 있어야 한다.

```nginx
server {
  listen 80;
  access_log /dev/stdout study;

  limit_req_status 429;
  limit_req_log_level notice;
  client_max_body_size 1k;  # 여기에 추가

  location ~* \.(?:css|js|png|jpe?g|gif|svg|ico)$ {
    # 기존 정적 파일 캐시 설정 ...
  }

  location / {
    # 기존 proxy·요청 제한 설정 ...
  }
}
```

`client_max_body_size`는 사용자가 보내는 요청 본문의 최대 크기다. 여기서는 결과를 쉽게 보기 위해 1KiB로 낮춘다.

**입력·실행 — 터미널 B**

```bash
# 호스트 파일에서 1KB 제한과 주변 중괄호를 함께 확인해 server 범위인지 판정한다.
grep -n -C 10 'client_max_body_size' nginx/conf.d/default.conf

# 수정한 본문 크기 제한 설정의 Nginx 문법이 올바른지 검사한다.
docker compose exec nginx nginx -t
# 문법 검사가 성공한 본문 크기 제한 설정을 실행 중인 Nginx에 반영한다.
docker compose exec nginx nginx -s reload

# 실행 중인 Nginx에서도 설정과 주변 중괄호를 함께 확인해 server 범위인지 판정한다.
docker compose exec nginx nginx -T 2>&1 | \
  grep -n -C 10 'client_max_body_size'
```

```bash
# 요청 제한 시험의 임시 제한 상태가 풀릴 때까지 3초 기다린다.
sleep 3
# 이전 시험 로그와 섞이지 않도록 이번 본문 크기 시험의 고유 ID를 만든다.
BODY_TEST_ID="step4-body-$(date +%s)-$$"
# 2KB 본문을 POST로 보내 Nginx가 1KB 제한을 초과한 요청을 413으로 차단하는지 확인한다.
dd if=/dev/zero bs=2048 count=1 2>/dev/null | tr '\0' 'a' | \
  curl -sS -o /dev/null -w 'HTTP %{http_code}\n' -X POST --data-binary @- \
  "http://localhost:8080/owners/new?study=$BODY_TEST_ID"
# 최근 Nginx 로그에서 해당 요청의 413 상태를 확인한다.
docker compose logs --since 1h --no-log-prefix nginx | \
  grep "study=$BODY_TEST_ID" | grep 'status=413'

# Tomcat 로그에 같은 요청이 없는지 확인해 Nginx 앞단에서 차단됐음을 검증한다.
docker compose exec -T app sh -c \
  "grep -h 'study=$BODY_TEST_ID' /tmp/tomcat/access/access_log*.log || true"
```

**2KiB 본문 생성 명령을 왼쪽부터 읽기**

```text
/dev/zero가 값이 0인 바이트를 계속 제공
→ dd가 그중 2,048바이트만 읽음
→ tr이 0 바이트를 문자 a로 교체
→ curl이 2,048개의 a를 POST 본문으로 전송
→ Nginx가 1KiB 제한과 비교해 413 반환
```

| 명령 구성 | 뜻 |
| --- | --- |
| `dd` | 지정한 입력에서 원하는 크기만큼 데이터를 복사하는 명령이다. 여기서는 시험용 2KiB 본문을 만든다. |
| `if=/dev/zero` | 입력을 `/dev/zero`로 지정한다. `/dev/zero`는 읽을 때마다 값이 0인 바이트를 계속 제공하는 리눅스 특수 장치다. |
| `bs=2048` | 한 블록의 크기를 2,048바이트로 정한다. |
| `count=1` | 블록을 한 개만 복사한다. 따라서 `2,048 × 1 = 2,048바이트`, 즉 2KiB가 만들어진다. |
| `2>/dev/null` | `dd`가 표준 오류로 출력하는 복사 통계는 필요하지 않아 버린다. 숫자 `2`는 표준 오류를 뜻한다. |
| 첫 번째 `\|` | `dd`가 만든 2,048바이트를 `tr`의 입력으로 전달한다. |
| `tr` | 입력에서 특정 값을 다른 문자로 바꾸는 명령이다. |
| `'\0'` | 값이 0인 바이트를 가리킨다. |
| `'a'` | 0 바이트를 문자 `a`로 교체한다. 결과는 `a` 2,048개로 이루어진 시험 본문이다. |
| 두 번째 `\|` | 변환한 2KiB 데이터를 `curl`의 표준 입력으로 전달한다. |
| `curl -sS` | 진행률은 숨기지만 실제 요청 오류는 표시한다. |
| `-o /dev/null` | Nginx가 반환한 HTML 응답 본문은 버린다. |
| `-w 'HTTP %{http_code}\n'` | 요청이 끝나면 `HTTP 413`처럼 상태 코드만 출력한다. |
| `-X POST` | HTTP 메서드를 POST로 지정한다. |
| `--data-binary @-` | 데이터를 가공하지 않고 그대로 요청 본문으로 사용한다. `@-`의 `-`는 앞 파이프에서 들어온 표준 입력을 읽으라는 뜻이다. |
| `study=$BODY_TEST_ID` | 이번 413 시험 요청을 Nginx·Tomcat 로그에서 찾기 위한 고유 ID다. |

**나머지 검사 명령 한눈에 보기**

| 핵심 구성 | 이 명령에서 하는 역할 |
| --- | --- |
| 첫 번째 `grep` | 편집한 호스트 파일에서 설정의 존재와 중괄호 위치를 함께 확인한다. |
| `nginx -T` 뒤의 `grep` | 실행 중인 Nginx에서도 같은 설정이 `server` 범위에 있는지 확인한다. |
| `sleep 3` | 앞의 속도 제한 시험이 사용한 요청 여유가 회복되기를 잠시 기다린다. |
| `BODY_TEST_ID=...` | 이전에 실패한 시험 로그와 이번 재시험 로그가 섞이지 않게 한다. |
| Nginx 로그 검색 | 최종 상태가 413인지 확인한다. |
| Tomcat 로그 검색 | 차단된 요청이 app까지 가지 않았는지 확인한다. |

**결과·로그 판정**

두 설정 검색에서 `client_max_body_size 1k;`가 첫 번째 `location`보다 위에 보여야 한다. 요청 결과는 `HTTP 413`, Nginx 로그는 `status=413`, Tomcat 검색은 출력 없음이 정상이다.

**짧은 설명**

`client_max_body_size`를 `server` 범위에 두면 모든 요청 경로의 본문 크기를 검사한다. 2KiB 본문은 학습용 1KiB 제한보다 크므로 Nginx에서 차단되고 app에는 도달하지 않는다.

### 4-3. 메서드·경로 차단

> **이 단계에서 검사하는 것:** 읽기 전용 경로의 GET은 허용하고 POST는 `405`, 비공개 경로는 `403`으로 Nginx가 직접 차단하는지 확인한다.

**입력·수정 — 터미널 B**

```bash
# 학습용 메서드·비공개 경로 차단 규칙을 추가하기 위해 Nginx 웹 서버 설정 파일을 연다.
nano nginx/conf.d/default.conf
```

nano 화면의 `server {}` 안에 다음 두 `location`을 추가하고 저장·종료한다.

```nginx
location = /study/read-only {
  if ($request_method !~ ^(GET|HEAD)$) { return 405; }
  return 204;
}

location = /study/private {
  return 403;
}
```

**설정 한눈에 보기**

| 설정 | 이 실습에서 하는 역할 |
| --- | --- |
| `location =` | 주소가 정확히 일치하는 요청만 처리한다. |
| `$request_method` | 요청의 GET·POST 같은 HTTP 메서드를 뜻한다. |
| `return 405` | GET·HEAD 이외의 메서드를 허용하지 않는다고 응답한다. |
| `return 204` | 허용된 읽기 요청에는 본문 없이 성공으로 응답한다. |
| `return 403` | 학습용 비공개 경로의 접근을 거부한다. |

**입력·실행 — 터미널 B**

```bash
# 수정한 메서드·경로 차단 설정의 Nginx 문법이 올바른지 검사한다.
docker compose exec nginx nginx -t
# 문법 검사가 성공한 메서드·경로 차단 설정을 실행 중인 Nginx에 반영한다.
docker compose exec nginx nginx -s reload

# 읽기 전용 학습 경로에 GET을 보내 본문 없는 정상 응답 204를 확인한다.
curl -i 'http://localhost:8080/study/read-only?study=step4-read'
# 같은 읽기 전용 경로에 POST를 보내 405로 차단되는지 확인한다.
curl -i -X POST --data 'study=true' \
  'http://localhost:8080/study/read-only?study=step4-method'
# 비공개 학습 경로에 요청해 Nginx가 403으로 차단하는지 확인한다.
curl -i 'http://localhost:8080/study/private?study=step4-private'

# Nginx 로그에서 GET 204·POST 405·비공개 403 요청을 함께 찾는다.
docker compose logs --since 2m --no-log-prefix nginx | \
  grep -E 'study=step4-(read|method|private)'

# Tomcat 로그에서 차단된 POST·비공개 요청이 app에 도달하지 않았는지 확인한다.
docker compose exec -T app sh -c \
  "grep -hE 'study=step4-(read|method|private)' /tmp/tomcat/access/access_log*.log || true"
```

**명령 한눈에 보기**

| 전체 명령·핵심 구성 | 확인하는 것 |
| --- | --- |
| `curl -i` | 응답 본문과 함께 HTTP 상태·응답 헤더도 출력한다. |
| 첫 번째 `curl` | 별도 메서드를 지정하지 않아 기본 GET 요청을 보내고 204인지 확인한다. |
| `-X POST` | 두 번째 요청의 HTTP 메서드를 POST로 지정한다. |
| `--data 'study=true'` | POST 요청 본문에 `study=true` 문자열을 넣는다. |
| 두 번째 `curl` | POST 요청이 405로 차단되는지 확인한다. |
| 세 번째 `curl` | 비공개 경로가 403인지 확인한다. |
| Nginx 로그 검색 | 세 요청과 각 상태 코드가 기록됐는지 확인한다. |
| Tomcat 로그 검색 | Nginx가 직접 처리해 app에는 전달하지 않았는지 확인한다. |

**결과·로그 판정**

```text
HTTP/1.1 204 No Content
HTTP/1.1 405 Not Allowed
HTTP/1.1 403 Forbidden
```

마지막 Tomcat 검색 명령은 아무것도 출력하지 않아야 한다.

**짧은 설명**

PetClinic의 실제 기능을 건드리지 않도록 Nginx 전용 연습 경로를 만든다. 세 요청은 Nginx가 직접 응답하므로 Nginx 로그에는 남지만 Tomcat 로그에는 남지 않는다.

### 4-4. 버전 숨김과 보안 헤더

> **이 단계에서 검사하는 것:** 응답에서 Nginx 상세 버전이 숨겨지고, 일반 화면과 정적 파일에 기본 보안 헤더가 모두 붙는지 확인한다.

**입력·수정 — 터미널 B**

```bash
# Nginx 버전 숨김과 보안 응답 헤더를 추가하기 위해 웹 서버 설정 파일을 연다.
nano nginx/conf.d/default.conf
```

nano 화면의 `server {}` 안에 다음 설정을 추가한다.

```nginx
server_tokens off;
add_header X-Content-Type-Options "nosniff" always;
add_header X-Frame-Options "SAMEORIGIN" always;
add_header Referrer-Policy "strict-origin-when-cross-origin" always;
```

**설정 한눈에 보기**

| 설정 | 이 실습에서 하는 역할 |
| --- | --- |
| `server_tokens off` | 응답의 `Server` 헤더에서 구체적인 Nginx 버전 번호를 숨긴다. |
| `X-Content-Type-Options` | 브라우저가 파일 형식을 임의로 추측하지 않게 한다. |
| `X-Frame-Options` | 다른 사이트가 PetClinic 화면을 함부로 프레임에 넣는 것을 제한한다. |
| `Referrer-Policy` | 다른 사이트로 전달되는 현재 페이지 주소 정보를 줄인다. |
| `always` | 오류 응답에도 해당 헤더를 붙인다. |

3단계에서 만든 정적 파일 `location`에는 이미 `X-Cache-Status`가 있다. 상위 헤더가 빠지지 않도록 같은 보안 헤더 세 줄을 그 `location`에도 추가한다.

같은 nano 화면에서 정적 파일 `location` 안의 `add_header X-Cache-Status` 아래에도 다음 세 줄을 추가한 뒤 저장·종료한다.

```nginx
add_header X-Content-Type-Options "nosniff" always;
add_header X-Frame-Options "SAMEORIGIN" always;
add_header Referrer-Policy "strict-origin-when-cross-origin" always;
```

**입력·실행 — 터미널 B**

```bash
# 수정한 버전 숨김·보안 헤더 설정의 Nginx 문법이 올바른지 검사한다.
docker compose exec nginx nginx -t
# 문법 검사가 성공한 보안 설정을 실행 중인 Nginx에 반영한다.
docker compose exec nginx nginx -s reload

# IMAGE_PATH가 없으면 기본 PetClinic 이미지 경로를 사용한다.
IMAGE_PATH=${IMAGE_PATH:-resources/images/pets.png}
# 정적 파일의 캐시·보안 헤더를 시험할 전체 URL을 만든다.
ASSET="http://localhost:8080/$IMAGE_PATH?study=step4-static"

# 일반 PetClinic 화면에서 Nginx 버전과 세 보안 헤더를 확인한다.
curl -sS -D - -o /dev/null http://localhost:8080/ | \
  grep -iE '^(server|x-content-type-options|x-frame-options|referrer-policy):'

# 정적 이미지 응답에도 캐시 헤더와 세 보안 헤더가 모두 있는지 확인한다.
curl -sS -D - -o /dev/null "$ASSET" | \
  grep -iE '^(x-cache-status|x-content-type-options|x-frame-options|referrer-policy):'
```

**명령 한눈에 보기**

| 핵심 구성 | 이 명령에서 하는 역할 |
| --- | --- |
| `IMAGE_PATH=${IMAGE_PATH:-...}` | 앞에서 변수를 만들지 않았으면 기본 이미지 경로를 사용한다. |
| `curl -sS` | 진행률은 숨기고 요청 오류는 표시한다. |
| `-D -` | 응답 헤더를 터미널에 출력한다. |
| `-o /dev/null` | 응답 본문은 버려 헤더만 확인하기 쉽게 만든다. |
| 첫 번째 `grep` | 일반 화면의 Server·보안 헤더를 확인한다. |
| 두 번째 `grep` | 정적 이미지의 캐시·보안 헤더를 확인한다. |

**결과·로그 판정**

`Server: nginx`처럼 버전 숫자가 없어야 한다. 일반 화면과 정적 이미지에는 보안 헤더 세 개가 모두 있고, 정적 이미지에는 `X-Cache-Status`도 있어야 한다.

**짧은 설명**

버전 번호는 불필요한 서버 정보를 줄이고, 세 보안 헤더는 브라우저의 파일 형식 추측·외부 프레임 삽입·상세 경로 전달을 제한한다. 정적 파일 `location`에 별도 `add_header`가 있으므로 같은 보안 헤더를 그 안에도 명시한다.

### 4-5. 학습용 제한 설정 정리

> **이 단계에서 검사하는 것:** 결과를 쉽게 보기 위해 낮춘 요청 제한값을 제거하거나 팀 기준으로 되돌리고, 일반 PetClinic 사용이 다시 정상인지 확인한다.

`rate=5r/s`와 `client_max_body_size 1k`는 결과를 쉽게 확인하기 위한 값이다. 5단계로 넘어가기 전에 다음처럼 정리한다.

#### 4-5-1. `00-observability.conf` 전체 내용으로 교체

요청 속도 제한 시험이 끝났으므로 `limit_req_zone`을 제거한다. 로그 형식과 캐시 저장소는 이후에도 사용하므로 그대로 남긴다.

**입력·수정 — 터미널 B · 관찰 설정 파일**

```bash
# 학습용 요청 제한 기준을 제거한 최종 내용으로 교체하기 위해 관찰 설정 파일을 연다.
nano nginx/conf.d/00-observability.conf
```

nano 화면의 기존 내용을 모두 지우고 아래 전체 내용을 붙여 넣은 뒤 `Ctrl+O → Enter → Ctrl+X`로 저장·종료한다.

```nginx
log_format study 'time=$time_iso8601 status=$status method=$request_method '
                 'uri="$request_uri" cache=$upstream_cache_status '
                 'rate_limit=$limit_req_status request_time=$request_time '
                 'upstream_connect=$upstream_connect_time '
                 'upstream_header=$upstream_header_time '
                 'upstream_time=$upstream_response_time upstream="$upstream_addr"';
error_log /dev/stderr notice;

proxy_cache_path /var/cache/nginx levels=1:2 keys_zone=petclinic_cache:10m
                 inactive=10m max_size=100m use_temp_path=off;
```

이 최종 내용에는 `limit_req_zone`이 없다. `log_format`과 `proxy_cache_path`는 삭제하지 않는다.

#### 4-5-2. `default.conf` 전체 내용으로 교체

요청 속도 제한 설정과 연습용 차단 경로는 제거하고, 본문 크기 제한은 일반적인 `1m`으로 되돌린다. 캐시·버전 숨김·보안 헤더·리버스 프록시는 유지한다.

**입력·수정 — 터미널 B · 웹 서버 설정 파일**

```bash
# 학습용 제한을 정리한 최종 내용으로 교체하기 위해 Nginx 웹 서버 설정 파일을 연다.
nano nginx/conf.d/default.conf
```

nano 화면의 기존 내용을 모두 지우고 아래 전체 내용을 붙여 넣은 뒤 `Ctrl+O → Enter → Ctrl+X`로 저장·종료한다.

```nginx
server {
  listen 80;
  access_log /dev/stdout study;

  server_tokens off;
  client_max_body_size 1m;

  add_header X-Content-Type-Options "nosniff" always;
  add_header X-Frame-Options "SAMEORIGIN" always;
  add_header Referrer-Policy "strict-origin-when-cross-origin" always;

  location ~* \.(?:css|js|png|jpe?g|gif|svg|ico)$ {
    proxy_pass http://app:8080;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;

    proxy_cache petclinic_cache;
    proxy_cache_valid 200 5m;
    proxy_cache_methods GET HEAD;
    proxy_cache_bypass $arg_bypass_cache;
    proxy_no_cache $arg_bypass_cache;

    add_header X-Cache-Status $upstream_cache_status always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header Referrer-Policy "strict-origin-when-cross-origin" always;
  }

  location / {
    proxy_pass http://app:8080;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
  }
}
```

| 최종 상태 | 정리하는 이유 |
| --- | --- |
| `client_max_body_size 1m` | 1KiB 제한 때문에 정상 폼 요청까지 막히는 것을 방지한다. |
| 요청 속도 제한 설정 없음 | 학습용 초당 5회 제한을 종료한다. |
| `/study/read-only`·`/study/private` 없음 | 시험용으로 만든 가상 경로를 제거한다. |
| 캐시·버전 숨김·보안 헤더 유지 | 다음 장애 실습과 일반 PetClinic 사용에서도 계속 적용한다. |

#### 4-5-3. 두 파일 검사·반영

**입력·실행 — 터미널 B**

```bash
# 제한값을 정리한 뒤 Nginx 설정 문법이 올바른지 검사한다.
docker compose exec nginx nginx -t
# 문법 검사가 성공한 정리 설정을 실행 중인 Nginx에 반영한다.
docker compose exec nginx nginx -s reload
```

**명령·설정 한눈에 보기:** `nginx -t`는 정리한 두 파일의 문법을 검사하고, `nginx -s reload`는 성공한 설정만 실행 중인 Nginx에 다시 읽힌다.

**결과·로그 판정**

`test is successful`이 나오고 reload가 오류 없이 끝나야 한다. PC 브라우저에서 PetClinic 첫 화면이 다시 열리면 정상이다.

**짧은 설명**

새 기능을 추가하는 단계가 아니라, 429·413·405·403을 쉽게 재현하려고 사용한 학습용 제한을 정리하는 단계다. 캐시·로그·보안 헤더는 유지한다.

---

## 5. PetClinic 장애 확인·안내·복구

### 5-1. 확인할 장애 상황

> **이 단계에서 검사하는 것:** app이 중지됐을 때도 Nginx는 살아 있지만 전달 대상이 없어 `502`가 발생한다는 장애 흐름과 성공 기준을 먼저 확인한다.

이번에는 Nginx는 실행한 채 `app`만 멈춘다. Nginx access log의 `502`로 사용자가 받은 결과를 확인하고, error log의 upstream 연결 오류로 원인을 확인한다.

```text
브라우저 → Nginx: 연결 성공
Nginx → app:8080: 연결 실패
브라우저 ← Nginx: 502 Bad Gateway
```

**결과·로그 판정**

app을 멈췄을 때 Nginx 응답은 502, access log는 `status=502`, error log는 app upstream 연결 실패를 보여야 한다.

**짧은 설명**

이 절은 다음 두 절에서 만들 오류 안내 화면과 장애 복구 실험의 판정 기준을 먼저 정한다. 실제 명령 입력은 5-2와 5-3에서 진행한다.

### 5-2. 사용자에게 보여 줄 오류 페이지 준비

> **이 단계에서 검사하는 것:** app 장애 시 HTTP 상태는 `502`로 유지하면서 기본 오류 화면 대신 이해하기 쉬운 안내 화면을 Nginx가 보여 주도록 준비한다.

**입력·수정 — 터미널 B · Compose 파일**

```bash
# Nginx에 사용자 안내용 오류 HTML을 연결하기 위해 Compose 파일을 연다.
nano docker-compose.yml
```

nano 화면에서 `services.nginx.volumes` 아래에 다음 한 줄을 추가하고 저장·종료한다.

```yaml
- ./nginx/html:/usr/share/nginx/html:ro
```

**설정 한눈에 보기**

| 설정 | 이 실습에서 하는 역할 |
| --- | --- |
| `./nginx/html` | PetClinic 폴더에서 우리가 오류 HTML을 저장할 위치다. |
| `/usr/share/nginx/html` | Nginx 컨테이너가 HTML 파일을 읽을 위치다. |
| `ro` | 컨테이너에서는 이 폴더를 수정하지 못하고 읽기만 하게 한다. |

**입력·수정 — 터미널 B · 안내 HTML**

```bash
# 사용자 안내용 50x 오류 HTML을 보관할 폴더를 만든다.
mkdir -p nginx/html
```

```bash
# app 장애 때 보여 줄 사용자 안내 HTML 파일을 새로 작성한다.
nano nginx/html/study-50x.html
```

nano 화면에 아래 전체 내용을 입력하고 저장·종료한다.

```html
<!doctype html>
<html lang="ko">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>PetClinic 연결 대기</title>
  <style>
    body { margin: 0; background: #f5f7fb; color: #172033; font-family: sans-serif; }
    main { max-width: 640px; margin: 12vh auto; padding: 40px; background: white;
           border: 1px solid #d8deea; border-radius: 16px; }
    h1 { color: #1f5ea8; }
  </style>
</head>
<body>
  <main>
    <h1>잠시 PetClinic에 연결할 수 없습니다</h1>
    <p>Nginx는 정상 동작 중이지만 뒤쪽 애플리케이션이 응답하지 않습니다.</p>
    <p>잠시 후 다시 시도해 주세요.</p>
  </main>
</body>
</html>
```

**입력·수정 — 터미널 B · Nginx 설정**

```bash
# 같은 오류 페이지 설정을 이미 추가한 적이 있는지 먼저 찾아 중복을 막는다.
grep -n -C 2 -E 'proxy_intercept_errors|error_page|study-50x' nginx/conf.d/default.conf || true

# 502·503·504 발생 시 안내 HTML을 사용하도록 Nginx 웹 서버 설정 파일을 연다.
nano nginx/conf.d/default.conf
```

nano 화면에서 `server {}`와 기존 `location /`의 안내된 위치에 다음 설정을 추가하고 저장·종료한다. 이전에 같은 실습을 시도한 적이 있다면 새 줄을 또 추가하지 말고, 각 설정을 **정확히 한 번씩만** 남긴다. 특히 `proxy_intercept_errors on;`은 `location / { ... }` 안에 한 줄만 둔다.

```nginx
error_page 502 503 504 /study-50x.html;

location = /study-50x.html {
  root /usr/share/nginx/html;
  internal;
}

# 기존 location / 안에 추가
proxy_intercept_errors on;
```

**설정 한눈에 보기**

| 설정 | 이 실습에서 하는 역할 |
| --- | --- |
| `error_page 502 503 504` | 세 서버 오류가 발생하면 작성한 HTML을 사용한다. |
| `location = /study-50x.html` | 오류 HTML을 읽을 정확한 내부 경로를 만든다. |
| `root` | 오류 HTML 파일이 있는 컨테이너 폴더를 지정한다. |
| `internal` | 사용자가 HTML 주소를 직접 열지 못하고 오류 처리 중에만 사용하게 한다. |
| `proxy_intercept_errors` | app에서 받은 5xx 응답도 Nginx가 오류 페이지로 바꿔 보여 주게 한다. |

**입력·실행 — 터미널 B**

```bash
# 새 HTML 마운트까지 포함한 Nginx 설정을 임시 컨테이너에서 검사한다.
docker compose run --rm --no-deps nginx nginx -t
# Compose의 새 HTML 볼륨 연결을 적용하도록 Nginx 컨테이너를 다시 만든다.
docker compose up -d --force-recreate nginx
# internal 오류 페이지를 사용자가 주소로 직접 열 수 없는지 404 응답으로 확인한다.
curl -i http://localhost:8080/study-50x.html
```

**명령 한눈에 보기**

| 전체 명령·핵심 구성 | 하는 일 |
| --- | --- |
| `docker compose run ... nginx -t` | 새 HTML 마운트를 포함한 임시 컨테이너에서 Nginx 문법을 검사한다. |
| `up -d --force-recreate nginx` | Compose의 새 볼륨 연결을 적용하도록 Nginx 컨테이너를 다시 만든다. |
| `curl` | Nginx의 공개 주소로 HTTP 요청을 보낸다. |
| `-i` | 응답 본문 앞에 HTTP 상태와 응답 헤더도 함께 출력한다. |
| `/study-50x.html` | 오류 처리에만 쓰도록 만든 내부 페이지의 주소다. 직접 요청했을 때 404여야 한다. |

**결과·로그 판정**

`nginx -t`는 `test is successful`, 직접 `/study-50x.html`을 요청한 결과는 `HTTP/1.1 404 Not Found`여야 한다.

**짧은 설명**

호스트의 안내 HTML 폴더를 Nginx 컨테이너에 읽기 전용으로 연결한다. `internal`은 사용자가 파일을 직접 여는 것은 막고 실제 502·503·504 처리 때만 Nginx가 내부적으로 사용하게 한다. 화면만 바꾸며 HTTP 장애 상태는 그대로 유지한다.

### 5-3. app 중지와 복구 판정

> **이 단계에서 검사하는 것:** app 중지→502와 연결 실패 로그→app 재시작→200 복구의 전체 과정을 상태코드와 로그로 확인한다.

**입력·실행 — 터미널 B · 장애 발생과 원인 확인**

```bash
# 장애 상황을 재현하기 위해 PetClinic app 컨테이너만 중지한다.
docker compose stop app
# Nginx에 요청해 상태는 502이면서 사용자 안내 화면이 표시되는지 확인한다.
curl --max-time 20 -i 'http://localhost:8080/?study=step5-down'
# Nginx access log에서 방금 장애 요청의 502 상태를 확인한다.
docker compose logs --since 1m --no-log-prefix nginx | \
  grep 'study=step5-down' | grep 'status=502'
# Nginx error log에서 app 연결 실패 원인을 나타내는 문구를 찾는다.
docker compose logs --since 1m --no-log-prefix nginx | \
  grep -Ei 'connect\(\) failed|no live upstreams|upstream timed out|host not found in upstream'

# 장애 시험이 끝난 PetClinic app 컨테이너를 다시 시작한다.
docker compose start app
```

**명령 한눈에 보기**

| 전체 명령·핵심 구성 | 하는 일 |
| --- | --- |
| `stop app` | Nginx·PostgreSQL은 유지하고 PetClinic 컨테이너만 멈춘다. |
| `curl` | Nginx의 공개 주소로 HTTP 요청을 보낸다. |
| `--max-time 20` | 연결과 응답을 포함한 전체 요청이 20초를 넘으면 중단한다. |
| `-i` | 응답 본문 앞에 HTTP 상태와 응답 헤더도 함께 출력한다. |
| 첫 번째 로그 검색 | access log에서 사용자가 받은 `502`를 확인한다. |
| 두 번째 로그 검색 | error log에서 app 연결 실패 원인을 확인한다. |
| `start app` | 중지했던 PetClinic 컨테이너를 다시 시작한다. |

**입력·실행 — 터미널 A · app 복구 확인**

```bash
# 재시작한 app이 Started 상태까지 정상 복구되는지 실시간으로 확인한다.
docker compose logs -f --since 10s app
```

`Started ...`가 보이면 app 기동이 완료된 것이다. 터미널 A는 그대로 두거나 `Ctrl+C`로 로그 보기만 끝낸다.

**입력·실행 — 터미널 B · HTTP 복구 확인**

```bash
# app 복구 후 Nginx 요청이 다시 200으로 처리되는지 확인한다.
curl -i 'http://localhost:8080/?study=step5-up'
# 장애 때의 502와 복구 후 200을 Nginx 로그에서 함께 비교한다.
docker compose logs --since 2m --no-log-prefix nginx | \
  grep -E 'study=step5-(down|up)'
```

**명령 한눈에 보기**

| 핵심 구성 | 이 명령에서 하는 역할 |
| --- | --- |
| `curl` | 복구된 Nginx 공개 주소로 HTTP 요청을 보낸다. |
| `-i` | 응답 본문 앞에 HTTP 상태와 응답 헤더도 함께 출력해 `200`을 확인한다. |
| `?study=step5-up` | 복구 확인 요청을 이전 요청과 구분하기 위한 로그 표식이다. |
| `logs --since 2m nginx` | 최근 2분의 Nginx 로그를 가져온다. |
| `--no-log-prefix` | Compose가 붙이는 서비스명 접두사를 빼고 로그 본문만 보여 준다. |
| `grep -E '...(down\|up)'` | 장애 요청 `down`과 복구 요청 `up`이 들어간 두 로그를 함께 고른다. |

**결과·로그 판정**

| 시점 | HTTP·화면 | 로그 |
| --- | --- | --- |
| app 중지 후 | `502`, 작성한 안내 화면 | access log `status=502`, error log에 upstream 연결 실패 |
| app 재시작 후 | `200`, PetClinic 화면 | app `Started ...`, Nginx 로그에 app upstream 재등장 |

다른 앱으로 자동 우회하는 로드밸런싱은 5주차 범위다.

> app 로그에 `relation ... already exists`가 나오며 다시 종료되면 프록시 문제가 아니라 초기화 SQL 재실행 문제다. 3주차 DB 테이블과 `SPRING_SQL_INIT_MODE=never`를 확인한 뒤 app만 재생성하며, DB 볼륨은 삭제하지 않는다.

**짧은 설명**

Nginx는 살아 있어도 전달 대상 app이 멈추면 502를 반환한다. 안내 화면은 상황을 설명하지만 상태 코드는 502로 유지한다. access log는 사용자가 받은 결과를, error log는 `app:8080` 연결 실패 원인을 보여 준다. app을 재시작한 뒤 200과 upstream 로그가 돌아오면 복구된 것이다.

---

## 종료

**입력·실행 — 터미널 B**

```bash
# 실습 컨테이너와 네트워크를 종료하되 PostgreSQL 데이터 볼륨은 보존한다.
docker compose down
```

`-v`를 붙이지 않는다. 그러면 PostgreSQL Named Volume을 보존한 채 컨테이너만 내린다.

정상이라면 컨테이너가 `Stopped`, `Removed`되는 문구가 나오고, `docker compose ps`를 다시 실행했을 때 실행 중인 4주차 서비스가 보이지 않는다. 데이터 볼륨은 유지되므로 다음 실행 때 기존 데이터가 다시 연결된다.

| 최종 확인 | 성공 기준 |
| --- | --- |
| 1. 연결 | 세 서비스 실행, `nginx -t` 성공, Nginx 경유 응답 |
| 2. 비공개 | Nginx만 게시되고 app·postgres는 내부 네트워크에서 실행 |
| 3. 캐시 | 실제 이미지에서 `MISS → HIT`, 시간과 app 전달 횟수 감소 |
| 4. 보호 | `429/413/405/403` 및 보안 헤더를 각각 확인 |
| 5. 장애 | 안내 화면과 HTTP `502`, 원인 로그, app 재시작 후 정상 복구 |
