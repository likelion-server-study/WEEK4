# 4주차 결과 기록 — Nginx 앞단 구성과 관찰

> 아래 빈칸은 팀원이 실제로 실행한 값으로 채웁니다. 명령 전체를 복사하기보다, **무엇을 확인했고 어떤 결과가 나왔는지** 남기는 것이 목적입니다.

## 1. 실행 환경

| 항목 | 기록 |
| --- | --- |
| 이름 |  |
| 날짜 |  |
| 운영체제 | Ubuntu(WSL2) / macOS / 기타: |
| Docker 버전 |  |
| Docker Compose 버전 |  |
| PetClinic 이미지 이름·태그 |  |
| 실제 Compose 서비스명 | app: / postgres: / nginx: |

## 2. 최종 구조 확인

```text
브라우저 → localhost:_____ → Nginx → _____:8080 → PetClinic → _____:5432 → PostgreSQL
```

| 확인 항목 | 내 결과 | 판정 |
| --- | --- | --- |
| `docker compose ps`에서 nginx·app·postgres 실행 상태 |  | 성공 / 확인 필요 |
| 호스트에 공개된 포트 |  | Nginx만 공개 / 확인 필요 |
| `localhost`로 PetClinic 접속 | 상태 코드: | 성공 / 실패 |
| `localhost:8080` 직접 접속 | 결과: | 차단됨 / 열려 있음 |
| `localhost:5432` 직접 접속 | 결과: | 차단됨 / 열려 있음 |
| `docker compose exec nginx nginx -t` | 결과: | 성공 / 실패 |

`docker compose ps` 출력 또는 스크린샷 위치:

```text

```

## 3. Nginx가 앱에 요청을 전달하는지

| 항목 | 기록 |
| --- | --- |
| Nginx 설정의 `proxy_pass` 대상 |  |
| Nginx 경유 첫 화면 요청 상태 코드 |  |
| Nginx access log의 `upstream` 값 |  |
| PetClinic 로그에서 같은 시간대 요청이 보였는가 | 예 / 아니오 |

Nginx 로그 한 줄:

```text

```

## 4. 정적 파일 캐시 비교

테스트한 실제 정적 파일 경로:

```text

```

같은 `study` 값과 같은 URL을 사용했는지: 예 / 아니오

| 조건 | 단일 요청 시간 | 100회 전체 시간 | 로그에서 센 상태 | PetClinic까지 전달된 횟수 |
| --- | ---: | ---: | --- | ---: |
| 캐시 우회 (`bypass_cache=1`) |  |  | BYPASS: |  |
| 캐시 첫 요청 |  | - | MISS: |  |
| 같은 URL 반복 요청 |  |  | HIT: |  |

응답 헤더(`X-Cache-Status`) 기록:

```text
첫 요청:
두 번째 요청:
```

시간 차이가 작아도 괜찮습니다. 이 실습에서 더 중요한 근거는 **BYPASS/MISS에는 upstream 정보가 있고, HIT에는 PetClinic 전달이 없다는 것**입니다.

## 5. 앞단 방어 설정 결과

| 설정 | 시험한 요청 | 기대 상태 | 실제 상태 | Nginx 로그 확인 |
| --- | --- | --- | --- | --- |
| 요청 속도 제한 | 짧은 시간에 병렬 요청 | 일부 `429` |  | 예 / 아니오 |
| 서버 버전 숨기기 | `Server` 헤더 확인 | 버전 번호 없음 |  | 예 / 아니오 |
| 보안 헤더 | `X-Content-Type-Options` 등 확인 | 헤더 존재 |  | 예 / 아니오 |
| 본문 크기 제한 (선택) | 2 KiB POST | `413` |  | 예 / 아니오 |
| 메서드 제한 (선택) | 읽기 전용 경로 TRACE | `405` |  | 예 / 아니오 |

429 또는 413/405 응답·로그 일부:

```text

```

## 6. 장애와 복구

| 단계 | 결과 |
| --- | --- |
| `docker compose stop app` 뒤 Nginx 경유 요청 상태 |  |
| Nginx error log의 upstream 오류 메시지 |  |
| `docker compose start app` 뒤 복구된 상태 코드 |  |
| 앱이 준비되기까지 관찰한 점 |  |

502 관련 로그 일부:

```text

```

## 7. 설정 변경과 복구

| 항목 | 기록 |
| --- | --- |
| 설정 백업 파일 경로 |  |
| `nginx -t` 실패를 한 번 재현했는가 (선택) | 예 / 아니오 |
| 오류 메시지에서 확인한 파일·줄 |  |
| 원상 복구 후 `nginx -t` 결과 |  |
| reload를 썼는지, 컨테이너 재생성을 썼는지 |  |

## 8. 이번 주에 이해한 것과 남은 질문

### 이번에 확인한 것

- 
- 

### 아직 헷갈리는 것 또는 다음에 확인할 것

- 
- 

### 5주차로 이어질 질문

앱 컨테이너가 두 개 이상이라면, Nginx는 어떤 기준으로 요청을 나눌까?

